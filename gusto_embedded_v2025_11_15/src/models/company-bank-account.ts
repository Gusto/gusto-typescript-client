/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../lib/primitives.js";
import { safeParse } from "../lib/schemas.js";
import * as openEnums from "../types/enums.js";
import { OpenEnum } from "../types/enums.js";
import { Result as SafeParseResult } from "../types/fp.js";
import * as types from "../types/primitives.js";
import { SDKValidationError } from "./errors/sdk-validation-error.js";

/**
 * Bank account type
 */
export const CompanyBankAccountAccountType = {
  Checking: "Checking",
  Savings: "Savings",
} as const;
/**
 * Bank account type
 */
export type CompanyBankAccountAccountType = OpenEnum<
  typeof CompanyBankAccountAccountType
>;

/**
 * The verification status of the bank account.
 *
 * @remarks
 *
 * 'awaiting_deposits' means the bank account is just created and money is being transferred.
 * 'ready_for_verification' means the micro-deposits are completed and the verification process can begin by using the verify endpoint.
 * 'verified' means the bank account is verified.
 */
export const VerificationStatus = {
  AwaitingDeposits: "awaiting_deposits",
  ReadyForVerification: "ready_for_verification",
  Verified: "verified",
} as const;
/**
 * The verification status of the bank account.
 *
 * @remarks
 *
 * 'awaiting_deposits' means the bank account is just created and money is being transferred.
 * 'ready_for_verification' means the micro-deposits are completed and the verification process can begin by using the verify endpoint.
 * 'verified' means the bank account is verified.
 */
export type VerificationStatus = OpenEnum<typeof VerificationStatus>;

/**
 * The verification type of the bank account.
 *
 * @remarks
 *
 * 'bank_deposits' means the bank account is connected by entering routing and accounting numbers and verifying through micro-deposits.
 * 'plaid' means the bank account is connected through Plaid.
 */
export const VerificationType = {
  BankDeposits: "bank_deposits",
  Plaid: "plaid",
  PlaidExternal: "plaid_external",
} as const;
/**
 * The verification type of the bank account.
 *
 * @remarks
 *
 * 'bank_deposits' means the bank account is connected by entering routing and accounting numbers and verifying through micro-deposits.
 * 'plaid' means the bank account is connected through Plaid.
 */
export type VerificationType = OpenEnum<typeof VerificationType>;

export const PlaidStatus = {
  Connected: "connected",
  Disconnected: "disconnected",
} as const;
export type PlaidStatus = OpenEnum<typeof PlaidStatus>;

/**
 * The company bank account
 */
export type CompanyBankAccount = {
  /**
   * UUID of the bank account
   */
  uuid: string;
  /**
   * UUID of the company
   */
  companyUuid?: string | undefined;
  /**
   * Bank account type
   */
  accountType?: CompanyBankAccountAccountType | undefined;
  /**
   * The bank account's routing number
   */
  routingNumber?: string | undefined;
  /**
   * Masked bank account number
   */
  hiddenAccountNumber?: string | undefined;
  /**
   * The verification status of the bank account.
   *
   * @remarks
   *
   * 'awaiting_deposits' means the bank account is just created and money is being transferred.
   * 'ready_for_verification' means the micro-deposits are completed and the verification process can begin by using the verify endpoint.
   * 'verified' means the bank account is verified.
   */
  verificationStatus?: VerificationStatus | undefined;
  /**
   * The verification type of the bank account.
   *
   * @remarks
   *
   * 'bank_deposits' means the bank account is connected by entering routing and accounting numbers and verifying through micro-deposits.
   * 'plaid' means the bank account is connected through Plaid.
   */
  verificationType?: VerificationType | undefined;
  /**
   * The Plaid connection status of the bank account. Only applies when verification type is Plaid.
   */
  plaidStatus?: PlaidStatus | null | undefined;
  /**
   * The last fetch balance for the bank account. Please be aware that this amount does not reflect the most up-to-date balance and only applies when the verification type is Plaid.
   */
  lastCachedBalance?: string | null | undefined;
  /**
   * The balance fetch date associated with the last_cached_balance. Only applies when verification type is Plaid.
   */
  balanceFetchedDate?: string | null | undefined;
  /**
   * Name of bank account
   */
  name?: string | undefined;
};

/** @internal */
export const CompanyBankAccountAccountType$inboundSchema: z.ZodMiniType<
  CompanyBankAccountAccountType,
  unknown
> = openEnums.inboundSchema(CompanyBankAccountAccountType);

/** @internal */
export const VerificationStatus$inboundSchema: z.ZodMiniType<
  VerificationStatus,
  unknown
> = openEnums.inboundSchema(VerificationStatus);

/** @internal */
export const VerificationType$inboundSchema: z.ZodMiniType<
  VerificationType,
  unknown
> = openEnums.inboundSchema(VerificationType);

/** @internal */
export const PlaidStatus$inboundSchema: z.ZodMiniType<PlaidStatus, unknown> =
  openEnums.inboundSchema(PlaidStatus);

/** @internal */
export const CompanyBankAccount$inboundSchema: z.ZodMiniType<
  CompanyBankAccount,
  unknown
> = z.pipe(
  z.object({
    uuid: types.string(),
    company_uuid: types.optional(types.string()),
    account_type: types.optional(CompanyBankAccountAccountType$inboundSchema),
    routing_number: types.optional(types.string()),
    hidden_account_number: types.optional(types.string()),
    verification_status: types.optional(VerificationStatus$inboundSchema),
    verification_type: types.optional(VerificationType$inboundSchema),
    plaid_status: z.optional(z.nullable(PlaidStatus$inboundSchema)),
    last_cached_balance: z.optional(z.nullable(types.string())),
    balance_fetched_date: z.optional(z.nullable(types.string())),
    name: types.optional(types.string()),
  }),
  z.transform((v) => {
    return remap$(v, {
      "company_uuid": "companyUuid",
      "account_type": "accountType",
      "routing_number": "routingNumber",
      "hidden_account_number": "hiddenAccountNumber",
      "verification_status": "verificationStatus",
      "verification_type": "verificationType",
      "plaid_status": "plaidStatus",
      "last_cached_balance": "lastCachedBalance",
      "balance_fetched_date": "balanceFetchedDate",
    });
  }),
);

export function companyBankAccountFromJSON(
  jsonString: string,
): SafeParseResult<CompanyBankAccount, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => CompanyBankAccount$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'CompanyBankAccount' from JSON`,
  );
}

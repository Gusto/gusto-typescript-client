# GetV1CompaniesCompanyIdPayrollsSortOrder

A string indicating whether to sort resulting events in ascending (asc) or descending (desc) chronological order. Events are sorted by their `timestamp`. Defaults to asc if left empty.

## Example Usage

```typescript
import { GetV1CompaniesCompanyIdPayrollsSortOrder } from "openapi/models/operations";

let value: GetV1CompaniesCompanyIdPayrollsSortOrder = "asc";
```

## Values

```typescript
"asc" | "desc"
```
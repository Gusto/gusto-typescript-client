"use strict";
/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */
Object.defineProperty(exports, "__esModule", { value: true });
exports.useTaxRequirementsGet = useTaxRequirementsGet;
exports.useTaxRequirementsGetSuspense = useTaxRequirementsGetSuspense;
exports.prefetchTaxRequirementsGet = prefetchTaxRequirementsGet;
exports.setTaxRequirementsGetData = setTaxRequirementsGetData;
exports.invalidateTaxRequirementsGet = invalidateTaxRequirementsGet;
exports.invalidateAllTaxRequirementsGet = invalidateAllTaxRequirementsGet;
exports.buildTaxRequirementsGetQuery = buildTaxRequirementsGetQuery;
exports.queryKeyTaxRequirementsGet = queryKeyTaxRequirementsGet;
const react_query_1 = require("@tanstack/react-query");
const taxRequirementsGet_js_1 = require("../funcs/taxRequirementsGet.js");
const primitives_js_1 = require("../lib/primitives.js");
const fp_js_1 = require("../types/fp.js");
const _context_js_1 = require("./_context.js");
/**
 * Get State Tax Requirements
 *
 * @remarks
 * Get all tax requirements for a given state.
 *
 * ### Metadata Examples
 *
 * ```json select
 * {
 *   "type": "select",
 *   "options": [
 *     { "label": "Semiweekly",  value: "Semi-weekly" },
 *     { "label": "Monthly",  value: "Monthly" },
 *     { "label": "Quarterly",  value: "Quarterly" },
 *   ]
 * }
 * ```
 * ```json radio
 * {
 *   "type": "radio",
 *   "options": [
 *     { "label": "No, we cannot reimburse",  value: false, short_label: "Not Reimbursable" },
 *     { "label": "Yes, we can reimburse",  value: true, short_label: "Reimbursable" },
 *   ]
 * }
 * ```
 * ```json account_number
 * {
 *   "type": "account_number",
 *   "mask": "######-##',
 *   "prefix": null
 * }
 * ```
 * ```json tax_rate
 * {
 *   "type": "tax_rate",
 *   "validation": {
 *     "type": "min_max",
 *     "min": "0.0004",
 *     "max": "0.081"
 *   }
 * }
 * ```
 *
 * scope: `company_tax_requirements:read`
 */
function useTaxRequirementsGet(request, options) {
    const client = (0, _context_js_1.useGustoEmbeddedContext)();
    return (0, react_query_1.useQuery)({
        ...buildTaxRequirementsGetQuery(client, request, options),
        ...options,
    });
}
/**
 * Get State Tax Requirements
 *
 * @remarks
 * Get all tax requirements for a given state.
 *
 * ### Metadata Examples
 *
 * ```json select
 * {
 *   "type": "select",
 *   "options": [
 *     { "label": "Semiweekly",  value: "Semi-weekly" },
 *     { "label": "Monthly",  value: "Monthly" },
 *     { "label": "Quarterly",  value: "Quarterly" },
 *   ]
 * }
 * ```
 * ```json radio
 * {
 *   "type": "radio",
 *   "options": [
 *     { "label": "No, we cannot reimburse",  value: false, short_label: "Not Reimbursable" },
 *     { "label": "Yes, we can reimburse",  value: true, short_label: "Reimbursable" },
 *   ]
 * }
 * ```
 * ```json account_number
 * {
 *   "type": "account_number",
 *   "mask": "######-##',
 *   "prefix": null
 * }
 * ```
 * ```json tax_rate
 * {
 *   "type": "tax_rate",
 *   "validation": {
 *     "type": "min_max",
 *     "min": "0.0004",
 *     "max": "0.081"
 *   }
 * }
 * ```
 *
 * scope: `company_tax_requirements:read`
 */
function useTaxRequirementsGetSuspense(request, options) {
    const client = (0, _context_js_1.useGustoEmbeddedContext)();
    return (0, react_query_1.useSuspenseQuery)({
        ...buildTaxRequirementsGetQuery(client, request, options),
        ...options,
    });
}
function prefetchTaxRequirementsGet(queryClient, client$, request) {
    return queryClient.prefetchQuery({
        ...buildTaxRequirementsGetQuery(client$, request),
    });
}
function setTaxRequirementsGetData(client, queryKeyBase, data) {
    const key = queryKeyTaxRequirementsGet(...queryKeyBase);
    return client.setQueryData(key, data);
}
function invalidateTaxRequirementsGet(client, queryKeyBase, filters) {
    return client.invalidateQueries({
        ...filters,
        queryKey: [
            "@gusto/embedded-api",
            "taxRequirements",
            "get",
            ...queryKeyBase,
        ],
    });
}
function invalidateAllTaxRequirementsGet(client, filters) {
    return client.invalidateQueries({
        ...filters,
        queryKey: ["@gusto/embedded-api", "taxRequirements", "get"],
    });
}
function buildTaxRequirementsGetQuery(client$, request, options) {
    return {
        queryKey: queryKeyTaxRequirementsGet(request.companyUuid, request.state, {
            scheduling: request.scheduling,
            xGustoAPIVersion: request.xGustoAPIVersion,
        }),
        queryFn: async function taxRequirementsGetQueryFn(ctx) {
            const sig = (0, primitives_js_1.combineSignals)(ctx.signal, options?.fetchOptions?.signal);
            const mergedOptions = {
                ...options,
                fetchOptions: { ...options?.fetchOptions, signal: sig },
            };
            return (0, fp_js_1.unwrapAsync)((0, taxRequirementsGet_js_1.taxRequirementsGet)(client$, request, mergedOptions));
        },
    };
}
function queryKeyTaxRequirementsGet(companyUuid, state, parameters) {
    return [
        "@gusto/embedded-api",
        "taxRequirements",
        "get",
        companyUuid,
        state,
        parameters,
    ];
}
//# sourceMappingURL=taxRequirementsGet.js.map
/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */
import { webhooksCreateSubscription } from "../funcs/webhooksCreateSubscription.js";
import { webhooksDeleteSubscription } from "../funcs/webhooksDeleteSubscription.js";
import { webhooksGetSubscription } from "../funcs/webhooksGetSubscription.js";
import { webhooksGetV1WebhooksHealthCheck } from "../funcs/webhooksGetV1WebhooksHealthCheck.js";
import { webhooksListSubscriptions } from "../funcs/webhooksListSubscriptions.js";
import { webhooksRequestVerificationToken } from "../funcs/webhooksRequestVerificationToken.js";
import { webhooksUpdateSubscription } from "../funcs/webhooksUpdateSubscription.js";
import { webhooksVerify } from "../funcs/webhooksVerify.js";
import { ClientSDK } from "../lib/sdks.js";
import { unwrapAsync } from "../types/fp.js";
export class Webhooks extends ClientSDK {
    /**
     * Create a webhook subscription
     *
     * @remarks
     * Create a webhook subscription to receive events of the specified subscription_types whenever there is a state change.
     *
     * > 📘 System Access Authentication
     * >
     * > This endpoint uses the [Bearer Auth scheme with the system-level access token in the HTTP Authorization header](https://docs.gusto.com/embedded-payroll/docs/system-access).
     *
     * scope: `webhook_subscriptions:write`
     */
    async createSubscription(security, request, options) {
        return unwrapAsync(webhooksCreateSubscription(this, security, request, options));
    }
    /**
     * List webhook subscriptions
     *
     * @remarks
     * Returns all webhook subscriptions associated with the provided Partner API token.
     *
     * > 📘 System Access Authentication
     * >
     * > This endpoint uses the [Bearer Auth scheme with the system-level access token in the HTTP Authorization header](https://docs.gusto.com/embedded-payroll/docs/system-access).
     *
     * scope: `webhook_subscriptions:read`
     */
    async listSubscriptions(security, request, options) {
        return unwrapAsync(webhooksListSubscriptions(this, security, request, options));
    }
    /**
     * Update a webhook subscription
     *
     * @remarks
     * Updates the Webhook Subscription associated with the provided UUID.
     *
     * > 📘 System Access Authentication
     * >
     * > This endpoint uses the [Bearer Auth scheme with the system-level access token in the HTTP Authorization header](https://docs.gusto.com/embedded-payroll/docs/system-access).
     *
     * scope: `webhook_subscriptions:write`
     */
    async updateSubscription(security, request, options) {
        return unwrapAsync(webhooksUpdateSubscription(this, security, request, options));
    }
    /**
     * Get a webhook subscription
     *
     * @remarks
     * Returns the Webhook Subscription associated with the provided UUID.
     *
     * > 📘 System Access Authentication
     * >
     * > This endpoint uses the [Bearer Auth scheme with the system-level access token in the HTTP Authorization header](https://docs.gusto.com/embedded-payroll/docs/system-access).
     *
     * scope: `webhook_subscriptions:read`
     */
    async getSubscription(security, request, options) {
        return unwrapAsync(webhooksGetSubscription(this, security, request, options));
    }
    /**
     * Delete a webhook subscription
     *
     * @remarks
     * Deletes the Webhook Subscription associated with the provided UUID.
     *
     * > 📘 System Access Authentication
     * >
     * > This endpoint uses the [Bearer Auth scheme with the system-level access token in the HTTP Authorization header](https://docs.gusto.com/embedded-payroll/docs/system-access).
     *
     * scope: `webhook_subscriptions:write`
     */
    async deleteSubscription(security, request, options) {
        return unwrapAsync(webhooksDeleteSubscription(this, security, request, options));
    }
    /**
     * Verify the webhook subscription
     *
     * @remarks
     * When a webhook subscription is created, a `verification_token` is POSTed to the registered webhook subscription URL. This `verify` endpoint needs to be called with `verification_token` before webhook events can be sent to the registered webhook URL.
     *
     * Use the /v1/webhook_subscriptions/{webhook_subscription_uuid}/request_verification_token API to resend the `verification_token` to the Subscriber.
     *
     * > 📘 System Access Authentication
     * >
     * > This endpoint uses the [Bearer Auth scheme with the system-level access token in the HTTP Authorization header](https://docs.gusto.com/embedded-payroll/docs/system-access).
     *
     * scope: `webhook_subscriptions:write`
     */
    async verify(security, request, options) {
        return unwrapAsync(webhooksVerify(this, security, request, options));
    }
    /**
     * Request the webhook subscription verification_token
     *
     * @remarks
     * Request that the webhook subscription `verification_token` be POSTed to the Subscription URL.
     *
     * > 📘 System Access Authentication
     * >
     * > This endpoint uses the [Bearer Auth scheme with the system-level access token in the HTTP Authorization header](https://docs.gusto.com/embedded-payroll/docs/system-access).
     *
     * scope: `webhook_subscriptions:read`
     */
    async requestVerificationToken(security, request, options) {
        return unwrapAsync(webhooksRequestVerificationToken(this, security, request, options));
    }
    /**
     * Get the webhooks health status
     *
     * @remarks
     * Returns the health status (`healthy`, `unhealthy`, or `unknown`) of the webhooks system based on the last ten minutes of activity.
     *
     * 📘 System Access Authentication
     *
     * This endpoint uses the [Bearer Auth scheme with the system-level access token in the HTTP Authorization header](https://docs.gusto.com/embedded-payroll/docs/system-access)
     *
     * scope: `webhook_subscriptions:read`
     */
    async getV1WebhooksHealthCheck(security, request, options) {
        return unwrapAsync(webhooksGetV1WebhooksHealthCheck(this, security, request, options));
    }
}
//# sourceMappingURL=webhooks.js.map
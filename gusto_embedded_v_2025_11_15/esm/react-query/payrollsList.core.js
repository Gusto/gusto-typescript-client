/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */
import { payrollsList } from "../funcs/payrollsList.js";
import { combineSignals } from "../lib/primitives.js";
import { unwrapAsync } from "../types/fp.js";
export function prefetchPayrollsList(queryClient, client$, request, options) {
    return queryClient.prefetchQuery({
        ...buildPayrollsListQuery(client$, request, options),
    });
}
export function buildPayrollsListQuery(client$, request, options) {
    return {
        queryKey: queryKeyPayrollsList(request.companyId, {
            xGustoAPIVersion: request.xGustoAPIVersion,
            processingStatuses: request.processingStatuses,
            payrollTypes: request.payrollTypes,
            processed: request.processed,
            includeOffCycle: request.includeOffCycle,
            include: request.include,
            startDate: request.startDate,
            endDate: request.endDate,
            dateFilterBy: request.dateFilterBy,
            page: request.page,
            per: request.per,
            sortOrder: request.sortOrder,
        }),
        queryFn: async function payrollsListQueryFn(ctx) {
            const sig = combineSignals(ctx.signal, options?.signal, options?.fetchOptions?.signal);
            const mergedOptions = {
                ...options?.fetchOptions,
                ...options,
                signal: sig,
            };
            return unwrapAsync(payrollsList(client$, request, mergedOptions));
        },
    };
}
export function queryKeyPayrollsList(companyId, parameters) {
    return ["@gusto/embedded-api", "Payrolls", "list", companyId, parameters];
}
//# sourceMappingURL=payrollsList.core.js.map
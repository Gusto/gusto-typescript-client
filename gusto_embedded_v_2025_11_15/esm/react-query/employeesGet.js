/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */
import { useQuery, useSuspenseQuery, } from "@tanstack/react-query";
import { useGustoEmbeddedContext } from "./_context.js";
import { buildEmployeesGetQuery, prefetchEmployeesGet, queryKeyEmployeesGet, } from "./employeesGet.core.js";
export { buildEmployeesGetQuery, prefetchEmployeesGet, queryKeyEmployeesGet, };
/**
 * Get an employee
 *
 * @remarks
 * Get an employee.
 *
 * Note: Compensation data (pay rate, payment unit, and related fields) represents sensitive employee pay information. When retrieving employee job data, these fields (`rate`, `payment_unit`, `current_compensation_uuid`, `compensations`) are returned only when the `compensations:read` scope is included. This allows you to access employee and job metadata without exposing pay rates.
 *
 * scope: `employees:read`
 */
export function useEmployeesGet(request, options) {
    const client = useGustoEmbeddedContext();
    return useQuery({
        ...buildEmployeesGetQuery(client, request, options),
        ...options,
    });
}
/**
 * Get an employee
 *
 * @remarks
 * Get an employee.
 *
 * Note: Compensation data (pay rate, payment unit, and related fields) represents sensitive employee pay information. When retrieving employee job data, these fields (`rate`, `payment_unit`, `current_compensation_uuid`, `compensations`) are returned only when the `compensations:read` scope is included. This allows you to access employee and job metadata without exposing pay rates.
 *
 * scope: `employees:read`
 */
export function useEmployeesGetSuspense(request, options) {
    const client = useGustoEmbeddedContext();
    return useSuspenseQuery({
        ...buildEmployeesGetQuery(client, request, options),
        ...options,
    });
}
export function setEmployeesGetData(client, queryKeyBase, data) {
    const key = queryKeyEmployeesGet(...queryKeyBase);
    return client.setQueryData(key, data);
}
export function invalidateEmployeesGet(client, queryKeyBase, filters) {
    return client.invalidateQueries({
        ...filters,
        queryKey: ["@gusto/embedded-api", "Employees", "get", ...queryKeyBase],
    });
}
export function invalidateAllEmployeesGet(client, filters) {
    return client.invalidateQueries({
        ...filters,
        queryKey: ["@gusto/embedded-api", "Employees", "get"],
    });
}
//# sourceMappingURL=employeesGet.js.map
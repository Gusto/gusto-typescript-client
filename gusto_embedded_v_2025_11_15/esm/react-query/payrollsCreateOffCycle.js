/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */
import { useMutation, } from "@tanstack/react-query";
import { payrollsCreateOffCycle } from "../funcs/payrollsCreateOffCycle.js";
import { combineSignals } from "../lib/primitives.js";
import { unwrapAsync } from "../types/fp.js";
import { useGustoEmbeddedContext } from "./_context.js";
/**
 * Create an off-cycle payroll
 *
 * @remarks
 * Creates a new, unprocessed, off-cycle payroll.
 *
 * ## `off_cycle_reason`
 * By default:
 * - External benefits and deductions will be included when the `off_cycle_reason` is set to `Correction`.
 * - All benefits and deductions are blocked when the `off_cycle_reason` is set to `Bonus`.
 *
 * These elections can be overridden with the `skip_regular_deductions` boolean.
 *
 * scope: `payrolls:run`
 */
export function usePayrollsCreateOffCycleMutation(options) {
    const client = useGustoEmbeddedContext();
    return useMutation({
        ...buildPayrollsCreateOffCycleMutation(client, options),
        ...options,
    });
}
export function mutationKeyPayrollsCreateOffCycle() {
    return ["@gusto/embedded-api", "Payrolls", "createOffCycle"];
}
export function buildPayrollsCreateOffCycleMutation(client$, hookOptions) {
    return {
        mutationKey: mutationKeyPayrollsCreateOffCycle(),
        mutationFn: function payrollsCreateOffCycleMutationFn({ request, options, }) {
            const mergedOptions = {
                ...hookOptions,
                ...options,
                fetchOptions: {
                    ...hookOptions?.fetchOptions,
                    ...options?.fetchOptions,
                    signal: combineSignals(hookOptions?.fetchOptions?.signal, options?.fetchOptions?.signal),
                },
            };
            return unwrapAsync(payrollsCreateOffCycle(client$, request, mergedOptions));
        },
    };
}
//# sourceMappingURL=payrollsCreateOffCycle.js.map
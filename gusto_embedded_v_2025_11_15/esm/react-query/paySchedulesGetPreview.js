/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */
import { useQuery, useSuspenseQuery, } from "@tanstack/react-query";
import { useGustoEmbeddedContext } from "./_context.js";
import { buildPaySchedulesGetPreviewQuery, prefetchPaySchedulesGetPreview, queryKeyPaySchedulesGetPreview, } from "./paySchedulesGetPreview.core.js";
export { buildPaySchedulesGetPreviewQuery, prefetchPaySchedulesGetPreview, queryKeyPaySchedulesGetPreview, };
/**
 * Preview pay schedule dates
 *
 * @remarks
 * Returns a preview of pay period dates and holidays for the given parameters (e.g. frequency, anchor pay date) for the next 18 months. Use this before creating or updating a pay schedule to show expected check dates and payroll deadlines.
 *
 * ### Related guides
 * - [Create a pay schedule](doc:create-a-pay-schedule)
 * - [Manage Pay Schedules via API](doc:manage-pay-schedules-api)
 *
 * scope: `pay_schedules:write`
 */
export function usePaySchedulesGetPreview(request, options) {
    const client = useGustoEmbeddedContext();
    return useQuery({
        ...buildPaySchedulesGetPreviewQuery(client, request, options),
        ...options,
    });
}
/**
 * Preview pay schedule dates
 *
 * @remarks
 * Returns a preview of pay period dates and holidays for the given parameters (e.g. frequency, anchor pay date) for the next 18 months. Use this before creating or updating a pay schedule to show expected check dates and payroll deadlines.
 *
 * ### Related guides
 * - [Create a pay schedule](doc:create-a-pay-schedule)
 * - [Manage Pay Schedules via API](doc:manage-pay-schedules-api)
 *
 * scope: `pay_schedules:write`
 */
export function usePaySchedulesGetPreviewSuspense(request, options) {
    const client = useGustoEmbeddedContext();
    return useSuspenseQuery({
        ...buildPaySchedulesGetPreviewQuery(client, request, options),
        ...options,
    });
}
export function setPaySchedulesGetPreviewData(client, queryKeyBase, data) {
    const key = queryKeyPaySchedulesGetPreview(...queryKeyBase);
    return client.setQueryData(key, data);
}
export function invalidatePaySchedulesGetPreview(client, queryKeyBase, filters) {
    return client.invalidateQueries({
        ...filters,
        queryKey: [
            "@gusto/embedded-api",
            "paySchedules",
            "getPreview",
            ...queryKeyBase,
        ],
    });
}
export function invalidateAllPaySchedulesGetPreview(client, filters) {
    return client.invalidateQueries({
        ...filters,
        queryKey: ["@gusto/embedded-api", "paySchedules", "getPreview"],
    });
}
//# sourceMappingURL=paySchedulesGetPreview.js.map
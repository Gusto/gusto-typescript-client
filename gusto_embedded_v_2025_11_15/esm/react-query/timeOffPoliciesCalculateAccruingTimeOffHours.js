/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */
import { useMutation, } from "@tanstack/react-query";
import { timeOffPoliciesCalculateAccruingTimeOffHours } from "../funcs/timeOffPoliciesCalculateAccruingTimeOffHours.js";
import { combineSignals } from "../lib/primitives.js";
import { unwrapAsync } from "../types/fp.js";
import { useGustoEmbeddedContext } from "./_context.js";
/**
 * Calculate accruing time off hours
 *
 * @remarks
 * Returns a list of accruing time off for each time off policy associated with the employee.
 *
 * Factors affecting the accrued hours:
 *
 * - the time off policy accrual method (whether they get pay per hour worked, per hour paid, with / without overtime, accumulate time off based on pay period / calendar year / anniversary)
 * - how many hours of work during this pay period
 * - how many hours of PTO / sick hours taken during this pay period (for per hour paid policies only)
 * - company pay schedule frequency (for per pay period)
 *
 * If none of the parameters is passed in, the accrued time off hour will be 0.
 *
 * scope: `payrolls:read`
 */
export function useTimeOffPoliciesCalculateAccruingTimeOffHoursMutation(options) {
    const client = useGustoEmbeddedContext();
    return useMutation({
        ...buildTimeOffPoliciesCalculateAccruingTimeOffHoursMutation(client, options),
        ...options,
    });
}
export function mutationKeyTimeOffPoliciesCalculateAccruingTimeOffHours() {
    return [
        "@gusto/embedded-api",
        "timeOffPolicies",
        "calculateAccruingTimeOffHours",
    ];
}
export function buildTimeOffPoliciesCalculateAccruingTimeOffHoursMutation(client$, hookOptions) {
    return {
        mutationKey: mutationKeyTimeOffPoliciesCalculateAccruingTimeOffHours(),
        mutationFn: function timeOffPoliciesCalculateAccruingTimeOffHoursMutationFn({ request, options, }) {
            const mergedOptions = {
                ...hookOptions,
                ...options,
                fetchOptions: {
                    ...hookOptions?.fetchOptions,
                    ...options?.fetchOptions,
                    signal: combineSignals(hookOptions?.fetchOptions?.signal, options?.fetchOptions?.signal),
                },
            };
            return unwrapAsync(timeOffPoliciesCalculateAccruingTimeOffHours(client$, request, mergedOptions));
        },
    };
}
//# sourceMappingURL=timeOffPoliciesCalculateAccruingTimeOffHours.js.map
/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */
import { useMutation, } from "@tanstack/react-query";
import { signatoriesUpdate } from "../funcs/signatoriesUpdate.js";
import { combineSignals } from "../lib/primitives.js";
import { unwrapAsync } from "../types/fp.js";
import { useGustoEmbeddedContext } from "./_context.js";
/**
 * Update a signatory
 *
 * @remarks
 * Updates a signatory that has been either invited or created. If the signatory has been created with minimal information through the [Invite a signatory](ref:post-v1-companies-company_uuid-signatories-invite) endpoint, then the first update must contain all attributes specified in the request body in order to start the identity verification process.
 *
 * ## Related guides
 * - [Signatory Events](doc:signatory-events)
 *
 * scope: `signatories:write`
 */
export function useSignatoriesUpdateMutation(options) {
    const client = useGustoEmbeddedContext();
    return useMutation({
        ...buildSignatoriesUpdateMutation(client, options),
        ...options,
    });
}
export function mutationKeySignatoriesUpdate() {
    return ["@gusto/embedded-api", "Signatories", "update"];
}
export function buildSignatoriesUpdateMutation(client$, hookOptions) {
    return {
        mutationKey: mutationKeySignatoriesUpdate(),
        mutationFn: function signatoriesUpdateMutationFn({ request, options, }) {
            const mergedOptions = {
                ...hookOptions,
                ...options,
                fetchOptions: {
                    ...hookOptions?.fetchOptions,
                    ...options?.fetchOptions,
                    signal: combineSignals(hookOptions?.fetchOptions?.signal, options?.fetchOptions?.signal),
                },
            };
            return unwrapAsync(signatoriesUpdate(client$, request, mergedOptions));
        },
    };
}
//# sourceMappingURL=signatoriesUpdate.js.map
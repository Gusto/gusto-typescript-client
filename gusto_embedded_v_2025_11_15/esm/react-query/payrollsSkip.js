/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */
import { useMutation, } from "@tanstack/react-query";
import { payrollsSkip } from "../funcs/payrollsSkip.js";
import { combineSignals } from "../lib/primitives.js";
import { unwrapAsync } from "../types/fp.js";
import { useGustoEmbeddedContext } from "./_context.js";
/**
 * Skip a payroll
 *
 * @remarks
 * Submits a $0 payroll for employees associated with the pay schedule to skip payroll. This submission is asynchronous and a successful request responds with a 202 HTTP status. Upon success, the payroll is transitioned to the `processed` state.
 *
 * If the company is blocked from running payroll due to issues like incomplete setup, missing information or other compliance issues, the response will be 422 Unprocessable Entity with a categorization of the blockers as described in the error responses.
 *
 * scope: `payrolls:run`
 */
export function usePayrollsSkipMutation(options) {
    const client = useGustoEmbeddedContext();
    return useMutation({
        ...buildPayrollsSkipMutation(client, options),
        ...options,
    });
}
export function mutationKeyPayrollsSkip() {
    return ["@gusto/embedded-api", "Payrolls", "skip"];
}
export function buildPayrollsSkipMutation(client$, hookOptions) {
    return {
        mutationKey: mutationKeyPayrollsSkip(),
        mutationFn: function payrollsSkipMutationFn({ request, options, }) {
            const mergedOptions = {
                ...hookOptions,
                ...options,
                fetchOptions: {
                    ...hookOptions?.fetchOptions,
                    ...options?.fetchOptions,
                    signal: combineSignals(hookOptions?.fetchOptions?.signal, options?.fetchOptions?.signal),
                },
            };
            return unwrapAsync(payrollsSkip(client$, request, mergedOptions));
        },
    };
}
//# sourceMappingURL=payrollsSkip.js.map
/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */
import { useQuery, useSuspenseQuery, } from "@tanstack/react-query";
import { useGustoEmbeddedContext } from "./_context.js";
import { buildPayrollsGetReceiptQuery, prefetchPayrollsGetReceipt, queryKeyPayrollsGetReceipt, } from "./payrollsGetReceipt.core.js";
export { buildPayrollsGetReceiptQuery, prefetchPayrollsGetReceipt, queryKeyPayrollsGetReceipt, };
/**
 * Get a single payroll receipt
 *
 * @remarks
 * Returns a payroll receipt.
 *
 * Notes:
 * * Hour and dollar amounts are returned as string representations of numeric decimals.
 * * Dollar amounts are represented to the cent.
 * * If no data has yet be inserted for a given field, it defaults to "0.00" (for fixed amounts).
 *
 * scope: `payrolls:read`
 */
export function usePayrollsGetReceipt(request, options) {
    const client = useGustoEmbeddedContext();
    return useQuery({
        ...buildPayrollsGetReceiptQuery(client, request, options),
        ...options,
    });
}
/**
 * Get a single payroll receipt
 *
 * @remarks
 * Returns a payroll receipt.
 *
 * Notes:
 * * Hour and dollar amounts are returned as string representations of numeric decimals.
 * * Dollar amounts are represented to the cent.
 * * If no data has yet be inserted for a given field, it defaults to "0.00" (for fixed amounts).
 *
 * scope: `payrolls:read`
 */
export function usePayrollsGetReceiptSuspense(request, options) {
    const client = useGustoEmbeddedContext();
    return useSuspenseQuery({
        ...buildPayrollsGetReceiptQuery(client, request, options),
        ...options,
    });
}
export function setPayrollsGetReceiptData(client, queryKeyBase, data) {
    const key = queryKeyPayrollsGetReceipt(...queryKeyBase);
    return client.setQueryData(key, data);
}
export function invalidatePayrollsGetReceipt(client, queryKeyBase, filters) {
    return client.invalidateQueries({
        ...filters,
        queryKey: [
            "@gusto/embedded-api",
            "Payrolls",
            "getReceipt",
            ...queryKeyBase,
        ],
    });
}
export function invalidateAllPayrollsGetReceipt(client, filters) {
    return client.invalidateQueries({
        ...filters,
        queryKey: ["@gusto/embedded-api", "Payrolls", "getReceipt"],
    });
}
//# sourceMappingURL=payrollsGetReceipt.js.map
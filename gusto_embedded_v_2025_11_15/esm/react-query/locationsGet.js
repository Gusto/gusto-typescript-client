/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */
import { useQuery, useSuspenseQuery, } from "@tanstack/react-query";
import { useGustoEmbeddedContext } from "./_context.js";
import { buildLocationsGetQuery, prefetchLocationsGet, queryKeyLocationsGet, } from "./locationsGet.core.js";
export { buildLocationsGetQuery, prefetchLocationsGet, queryKeyLocationsGet, };
/**
 * Get all company locations
 *
 * @remarks
 * Retrieves all company locations (addresses) associated with a company: mailing addresses, filing
 * addresses, or work locations. A single address may serve multiple, or all, purposes.
 *
 * Since all company locations are subsets of locations, use the Locations endpoints to
 * [get](ref:get-v1-locations-location_id) or [update](ref:put-v1-locations-location_id) an individual record.
 *
 * scope: `companies:read`
 */
export function useLocationsGet(request, options) {
    const client = useGustoEmbeddedContext();
    return useQuery({
        ...buildLocationsGetQuery(client, request, options),
        ...options,
    });
}
/**
 * Get all company locations
 *
 * @remarks
 * Retrieves all company locations (addresses) associated with a company: mailing addresses, filing
 * addresses, or work locations. A single address may serve multiple, or all, purposes.
 *
 * Since all company locations are subsets of locations, use the Locations endpoints to
 * [get](ref:get-v1-locations-location_id) or [update](ref:put-v1-locations-location_id) an individual record.
 *
 * scope: `companies:read`
 */
export function useLocationsGetSuspense(request, options) {
    const client = useGustoEmbeddedContext();
    return useSuspenseQuery({
        ...buildLocationsGetQuery(client, request, options),
        ...options,
    });
}
export function setLocationsGetData(client, queryKeyBase, data) {
    const key = queryKeyLocationsGet(...queryKeyBase);
    return client.setQueryData(key, data);
}
export function invalidateLocationsGet(client, queryKeyBase, filters) {
    return client.invalidateQueries({
        ...filters,
        queryKey: ["@gusto/embedded-api", "Locations", "get", ...queryKeyBase],
    });
}
export function invalidateAllLocationsGet(client, filters) {
    return client.invalidateQueries({
        ...filters,
        queryKey: ["@gusto/embedded-api", "Locations", "get"],
    });
}
//# sourceMappingURL=locationsGet.js.map
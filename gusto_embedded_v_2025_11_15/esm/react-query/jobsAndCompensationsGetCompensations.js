/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */
import { useQuery, useSuspenseQuery, } from "@tanstack/react-query";
import { useGustoEmbeddedContext } from "./_context.js";
import { buildJobsAndCompensationsGetCompensationsQuery, prefetchJobsAndCompensationsGetCompensations, queryKeyJobsAndCompensationsGetCompensations, } from "./jobsAndCompensationsGetCompensations.core.js";
export { buildJobsAndCompensationsGetCompensationsQuery, prefetchJobsAndCompensationsGetCompensations, queryKeyJobsAndCompensationsGetCompensations, };
/**
 * Get compensations for a job
 *
 * @remarks
 * Compensations contain information on how much is paid out for a job. Jobs may have many compensations, but only one that is active. The current compensation is the one with the most recent `effective_date`. By default the API returns only the current compensation - see the `include` query parameter for retrieving all compensations.
 *
 * Note: Currently the API does not support creating multiple compensations per job - creating a compensation with the same `job_uuid` as another will fail with a relevant error.
 *
 * Use `flsa_status` to determine if an employee is eligible for overtime.
 *
 * scope: `compensations:read`
 */
export function useJobsAndCompensationsGetCompensations(request, options) {
    const client = useGustoEmbeddedContext();
    return useQuery({
        ...buildJobsAndCompensationsGetCompensationsQuery(client, request, options),
        ...options,
    });
}
/**
 * Get compensations for a job
 *
 * @remarks
 * Compensations contain information on how much is paid out for a job. Jobs may have many compensations, but only one that is active. The current compensation is the one with the most recent `effective_date`. By default the API returns only the current compensation - see the `include` query parameter for retrieving all compensations.
 *
 * Note: Currently the API does not support creating multiple compensations per job - creating a compensation with the same `job_uuid` as another will fail with a relevant error.
 *
 * Use `flsa_status` to determine if an employee is eligible for overtime.
 *
 * scope: `compensations:read`
 */
export function useJobsAndCompensationsGetCompensationsSuspense(request, options) {
    const client = useGustoEmbeddedContext();
    return useSuspenseQuery({
        ...buildJobsAndCompensationsGetCompensationsQuery(client, request, options),
        ...options,
    });
}
export function setJobsAndCompensationsGetCompensationsData(client, queryKeyBase, data) {
    const key = queryKeyJobsAndCompensationsGetCompensations(...queryKeyBase);
    return client.setQueryData(key, data);
}
export function invalidateJobsAndCompensationsGetCompensations(client, queryKeyBase, filters) {
    return client.invalidateQueries({
        ...filters,
        queryKey: [
            "@gusto/embedded-api",
            "jobsAndCompensations",
            "getCompensations",
            ...queryKeyBase,
        ],
    });
}
export function invalidateAllJobsAndCompensationsGetCompensations(client, filters) {
    return client.invalidateQueries({
        ...filters,
        queryKey: [
            "@gusto/embedded-api",
            "jobsAndCompensations",
            "getCompensations",
        ],
    });
}
//# sourceMappingURL=jobsAndCompensationsGetCompensations.js.map
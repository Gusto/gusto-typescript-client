/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */
import { useQuery, useSuspenseQuery, } from "@tanstack/react-query";
import { useGustoEmbeddedContext } from "./_context.js";
import { buildEarningTypesListQuery, prefetchEarningTypesList, queryKeyEarningTypesList, } from "./earningTypesList.core.js";
export { buildEarningTypesListQuery, prefetchEarningTypesList, queryKeyEarningTypesList, };
/**
 * Get all earning types for a company
 *
 * @remarks
 * A payroll item in Gusto is associated to an earning type to name the type of earning described by the payroll item.
 *
 * #### Default Earning Type
 * Certain earning types are special because they have tax considerations. Those earning types are mostly the same for every company depending on its legal structure (LLC, Corporation, etc.)
 *
 * #### Custom Earning Type
 * Custom earning types are all the other earning types added specifically for a company.
 *
 * scope: `payrolls:read`
 */
export function useEarningTypesList(request, options) {
    const client = useGustoEmbeddedContext();
    return useQuery({
        ...buildEarningTypesListQuery(client, request, options),
        ...options,
    });
}
/**
 * Get all earning types for a company
 *
 * @remarks
 * A payroll item in Gusto is associated to an earning type to name the type of earning described by the payroll item.
 *
 * #### Default Earning Type
 * Certain earning types are special because they have tax considerations. Those earning types are mostly the same for every company depending on its legal structure (LLC, Corporation, etc.)
 *
 * #### Custom Earning Type
 * Custom earning types are all the other earning types added specifically for a company.
 *
 * scope: `payrolls:read`
 */
export function useEarningTypesListSuspense(request, options) {
    const client = useGustoEmbeddedContext();
    return useSuspenseQuery({
        ...buildEarningTypesListQuery(client, request, options),
        ...options,
    });
}
export function setEarningTypesListData(client, queryKeyBase, data) {
    const key = queryKeyEarningTypesList(...queryKeyBase);
    return client.setQueryData(key, data);
}
export function invalidateEarningTypesList(client, queryKeyBase, filters) {
    return client.invalidateQueries({
        ...filters,
        queryKey: ["@gusto/embedded-api", "earningTypes", "list", ...queryKeyBase],
    });
}
export function invalidateAllEarningTypesList(client, filters) {
    return client.invalidateQueries({
        ...filters,
        queryKey: ["@gusto/embedded-api", "earningTypes", "list"],
    });
}
//# sourceMappingURL=earningTypesList.js.map
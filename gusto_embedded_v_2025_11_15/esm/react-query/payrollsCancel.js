/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */
import { useMutation, } from "@tanstack/react-query";
import { payrollsCancel } from "../funcs/payrollsCancel.js";
import { combineSignals } from "../lib/primitives.js";
import { unwrapAsync } from "../types/fp.js";
import { useGustoEmbeddedContext } from "./_context.js";
/**
 * Cancel a payroll
 *
 * @remarks
 * Transitions a `processed` payroll back to the `unprocessed` state. A payroll can be canceled if it meets both criteria:
 * - `processed` is true
 * - Current time is earlier than 3:30pm PT on the payroll_deadline
 *
 * scope: `payrolls:run`
 */
export function usePayrollsCancelMutation(options) {
    const client = useGustoEmbeddedContext();
    return useMutation({
        ...buildPayrollsCancelMutation(client, options),
        ...options,
    });
}
export function mutationKeyPayrollsCancel() {
    return ["@gusto/embedded-api", "Payrolls", "cancel"];
}
export function buildPayrollsCancelMutation(client$, hookOptions) {
    return {
        mutationKey: mutationKeyPayrollsCancel(),
        mutationFn: function payrollsCancelMutationFn({ request, options, }) {
            const mergedOptions = {
                ...hookOptions,
                ...options,
                fetchOptions: {
                    ...hookOptions?.fetchOptions,
                    ...options?.fetchOptions,
                    signal: combineSignals(hookOptions?.fetchOptions?.signal, options?.fetchOptions?.signal),
                },
            };
            return unwrapAsync(payrollsCancel(client$, request, mergedOptions));
        },
    };
}
//# sourceMappingURL=payrollsCancel.js.map
/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */
import { useQuery, useSuspenseQuery, } from "@tanstack/react-query";
import { useGustoEmbeddedContext } from "./_context.js";
import { buildSignatoriesListQuery, prefetchSignatoriesList, queryKeySignatoriesList, } from "./signatoriesList.core.js";
export { buildSignatoriesListQuery, prefetchSignatoriesList, queryKeySignatoriesList, };
/**
 * Get the signatories for a company
 *
 * @remarks
 * Returns the signatories for a company. A company has at most one signatory.
 *
 * ## Related guides
 * - [Signatory Events](doc:signatory-events)
 *
 * scope: `signatories:read`
 */
export function useSignatoriesList(request, options) {
    const client = useGustoEmbeddedContext();
    return useQuery({
        ...buildSignatoriesListQuery(client, request, options),
        ...options,
    });
}
/**
 * Get the signatories for a company
 *
 * @remarks
 * Returns the signatories for a company. A company has at most one signatory.
 *
 * ## Related guides
 * - [Signatory Events](doc:signatory-events)
 *
 * scope: `signatories:read`
 */
export function useSignatoriesListSuspense(request, options) {
    const client = useGustoEmbeddedContext();
    return useSuspenseQuery({
        ...buildSignatoriesListQuery(client, request, options),
        ...options,
    });
}
export function setSignatoriesListData(client, queryKeyBase, data) {
    const key = queryKeySignatoriesList(...queryKeyBase);
    return client.setQueryData(key, data);
}
export function invalidateSignatoriesList(client, queryKeyBase, filters) {
    return client.invalidateQueries({
        ...filters,
        queryKey: ["@gusto/embedded-api", "Signatories", "list", ...queryKeyBase],
    });
}
export function invalidateAllSignatoriesList(client, filters) {
    return client.invalidateQueries({
        ...filters,
        queryKey: ["@gusto/embedded-api", "Signatories", "list"],
    });
}
//# sourceMappingURL=signatoriesList.js.map
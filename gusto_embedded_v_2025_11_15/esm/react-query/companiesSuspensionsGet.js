/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */
import { useQuery, useSuspenseQuery, } from "@tanstack/react-query";
import { useGustoEmbeddedContext } from "./_context.js";
import { buildCompaniesSuspensionsGetQuery, prefetchCompaniesSuspensionsGet, queryKeyCompaniesSuspensionsGet, } from "./companiesSuspensionsGet.core.js";
export { buildCompaniesSuspensionsGetQuery, prefetchCompaniesSuspensionsGet, queryKeyCompaniesSuspensionsGet, };
/**
 * Get suspensions for this company
 *
 * @remarks
 * Get existing suspension records for this company. A company may have multiple suspension records if they have suspended their Gusto account more than once.
 *
 * >📘 To check if company is already suspended
 * >
 * > To determine if a company is _currently_ suspended, use the `is_suspended` and `company_status` fields in the [Get a company](https://docs.gusto.com/embedded-payroll/reference/get-v1-companies) endpoint.
 *
 * scope: `company_suspensions:read`
 */
export function useCompaniesSuspensionsGet(request, options) {
    const client = useGustoEmbeddedContext();
    return useQuery({
        ...buildCompaniesSuspensionsGetQuery(client, request, options),
        ...options,
    });
}
/**
 * Get suspensions for this company
 *
 * @remarks
 * Get existing suspension records for this company. A company may have multiple suspension records if they have suspended their Gusto account more than once.
 *
 * >📘 To check if company is already suspended
 * >
 * > To determine if a company is _currently_ suspended, use the `is_suspended` and `company_status` fields in the [Get a company](https://docs.gusto.com/embedded-payroll/reference/get-v1-companies) endpoint.
 *
 * scope: `company_suspensions:read`
 */
export function useCompaniesSuspensionsGetSuspense(request, options) {
    const client = useGustoEmbeddedContext();
    return useSuspenseQuery({
        ...buildCompaniesSuspensionsGetQuery(client, request, options),
        ...options,
    });
}
export function setCompaniesSuspensionsGetData(client, queryKeyBase, data) {
    const key = queryKeyCompaniesSuspensionsGet(...queryKeyBase);
    return client.setQueryData(key, data);
}
export function invalidateCompaniesSuspensionsGet(client, queryKeyBase, filters) {
    return client.invalidateQueries({
        ...filters,
        queryKey: ["@gusto/embedded-api", "suspensions", "get", ...queryKeyBase],
    });
}
export function invalidateAllCompaniesSuspensionsGet(client, filters) {
    return client.invalidateQueries({
        ...filters,
        queryKey: ["@gusto/embedded-api", "suspensions", "get"],
    });
}
//# sourceMappingURL=companiesSuspensionsGet.js.map
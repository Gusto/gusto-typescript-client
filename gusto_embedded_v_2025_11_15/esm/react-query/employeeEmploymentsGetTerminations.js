/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */
import { useQuery, useSuspenseQuery, } from "@tanstack/react-query";
import { useGustoEmbeddedContext } from "./_context.js";
import { buildEmployeeEmploymentsGetTerminationsQuery, prefetchEmployeeEmploymentsGetTerminations, queryKeyEmployeeEmploymentsGetTerminations, } from "./employeeEmploymentsGetTerminations.core.js";
export { buildEmployeeEmploymentsGetTerminationsQuery, prefetchEmployeeEmploymentsGetTerminations, queryKeyEmployeeEmploymentsGetTerminations, };
/**
 * Get terminations for an employee
 *
 * @remarks
 * Terminations are created whenever an employee is scheduled to leave the company. The only things required are an effective date (their last day of work) and whether they should receive their wages in a one-off termination payroll or with the rest of the company.
 *
 * Note that some states require employees to receive their final wages within 24 hours (unless they consent otherwise,) in which case running a one-off payroll may be the only option.
 *
 * scope: `employments:read`
 */
export function useEmployeeEmploymentsGetTerminations(request, options) {
    const client = useGustoEmbeddedContext();
    return useQuery({
        ...buildEmployeeEmploymentsGetTerminationsQuery(client, request, options),
        ...options,
    });
}
/**
 * Get terminations for an employee
 *
 * @remarks
 * Terminations are created whenever an employee is scheduled to leave the company. The only things required are an effective date (their last day of work) and whether they should receive their wages in a one-off termination payroll or with the rest of the company.
 *
 * Note that some states require employees to receive their final wages within 24 hours (unless they consent otherwise,) in which case running a one-off payroll may be the only option.
 *
 * scope: `employments:read`
 */
export function useEmployeeEmploymentsGetTerminationsSuspense(request, options) {
    const client = useGustoEmbeddedContext();
    return useSuspenseQuery({
        ...buildEmployeeEmploymentsGetTerminationsQuery(client, request, options),
        ...options,
    });
}
export function setEmployeeEmploymentsGetTerminationsData(client, queryKeyBase, data) {
    const key = queryKeyEmployeeEmploymentsGetTerminations(...queryKeyBase);
    return client.setQueryData(key, data);
}
export function invalidateEmployeeEmploymentsGetTerminations(client, queryKeyBase, filters) {
    return client.invalidateQueries({
        ...filters,
        queryKey: [
            "@gusto/embedded-api",
            "employeeEmployments",
            "getTerminations",
            ...queryKeyBase,
        ],
    });
}
export function invalidateAllEmployeeEmploymentsGetTerminations(client, filters) {
    return client.invalidateQueries({
        ...filters,
        queryKey: ["@gusto/embedded-api", "employeeEmployments", "getTerminations"],
    });
}
//# sourceMappingURL=employeeEmploymentsGetTerminations.js.map
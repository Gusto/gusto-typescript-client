/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../lib/primitives.js";
import { ClosedEnum } from "../types/enums.js";

/**
 * The breakdown of the report. Use 'default' for no split.
 */
export const GeneralLedgerReportBodyAggregation = {
  Default: "default",
  Job: "job",
  Department: "department",
  Integration: "integration",
} as const;
/**
 * The breakdown of the report. Use 'default' for no split.
 */
export type GeneralLedgerReportBodyAggregation = ClosedEnum<
  typeof GeneralLedgerReportBodyAggregation
>;

/**
 * The kind of integration set up for the company. Required when `aggregation` is 'integration'. Must be null if `aggregation` is not 'integration'.
 */
export const IntegrationType = {
  Xero: "xero",
  Qbo: "qbo",
} as const;
/**
 * The kind of integration set up for the company. Required when `aggregation` is 'integration'. Must be null if `aggregation` is not 'integration'.
 */
export type IntegrationType = ClosedEnum<typeof IntegrationType>;

/**
 * Request body for generating a general ledger report. The report can be aggregated by different dimensions such as job or department.
 */
export type GeneralLedgerReportBody = {
  /**
   * The breakdown of the report. Use 'default' for no split.
   */
  aggregation: GeneralLedgerReportBodyAggregation;
  /**
   * The kind of integration set up for the company. Required when `aggregation` is 'integration'. Must be null if `aggregation` is not 'integration'.
   */
  integrationType?: IntegrationType | null | undefined;
};

/** @internal */
export const GeneralLedgerReportBodyAggregation$outboundSchema: z.ZodMiniEnum<
  typeof GeneralLedgerReportBodyAggregation
> = z.enum(GeneralLedgerReportBodyAggregation);

/** @internal */
export const IntegrationType$outboundSchema: z.ZodMiniEnum<
  typeof IntegrationType
> = z.enum(IntegrationType);

/** @internal */
export type GeneralLedgerReportBody$Outbound = {
  aggregation: string;
  integration_type?: string | null | undefined;
};

/** @internal */
export const GeneralLedgerReportBody$outboundSchema: z.ZodMiniType<
  GeneralLedgerReportBody$Outbound,
  GeneralLedgerReportBody
> = z.pipe(
  z.object({
    aggregation: GeneralLedgerReportBodyAggregation$outboundSchema,
    integrationType: z.optional(z.nullable(IntegrationType$outboundSchema)),
  }),
  z.transform((v) => {
    return remap$(v, {
      integrationType: "integration_type",
    });
  }),
);

export function generalLedgerReportBodyToJSON(
  generalLedgerReportBody: GeneralLedgerReportBody,
): string {
  return JSON.stringify(
    GeneralLedgerReportBody$outboundSchema.parse(generalLedgerReportBody),
  );
}

/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../lib/primitives.js";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import * as types from "../types/primitives.js";
import { SDKValidationError } from "./errors/sdk-validation-error.js";

export type NotFoundErrorObjectError = {
  /**
   * Specifies where the error occurs. Typically this key identifies the attribute/parameter related to the error.
   */
  errorKey: string;
  /**
   * Specifies the type of error. The category provides error groupings and can be used to build custom error handling in your integration.
   */
  category: string;
  /**
   * Provides details about the error - generally this message can be surfaced to an end user.
   */
  message?: string | undefined;
};

/** @internal */
export const NotFoundErrorObjectError$inboundSchema: z.ZodMiniType<
  NotFoundErrorObjectError,
  unknown
> = z.pipe(
  z.object({
    error_key: types.string(),
    category: types.string(),
    message: types.optional(types.string()),
  }),
  z.transform((v) => {
    return remap$(v, {
      "error_key": "errorKey",
    });
  }),
);

export function notFoundErrorObjectErrorFromJSON(
  jsonString: string,
): SafeParseResult<NotFoundErrorObjectError, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => NotFoundErrorObjectError$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'NotFoundErrorObjectError' from JSON`,
  );
}

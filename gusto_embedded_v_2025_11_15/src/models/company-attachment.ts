/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../lib/primitives.js";
import { safeParse } from "../lib/schemas.js";
import * as openEnums from "../types/enums.js";
import { OpenEnum } from "../types/enums.js";
import { Result as SafeParseResult } from "../types/fp.js";
import * as types from "../types/primitives.js";
import { SDKValidationError } from "./errors/sdk-validation-error.js";

/**
 * The category of the company attachment.
 *
 * @remarks
 * - `gep_notice`: A tax notice attachment
 * - `compliance`: A compliance attachment
 * - `other`: Any other attachment type
 */
export const CompanyAttachmentCategory = {
  GepNotice: "gep_notice",
  Compliance: "compliance",
  Other: "other",
} as const;
/**
 * The category of the company attachment.
 *
 * @remarks
 * - `gep_notice`: A tax notice attachment
 * - `compliance`: A compliance attachment
 * - `other`: Any other attachment type
 */
export type CompanyAttachmentCategory = OpenEnum<
  typeof CompanyAttachmentCategory
>;

/**
 * The company attachment
 */
export type CompanyAttachment = {
  /**
   * UUID of the company attachment
   */
  uuid: string;
  /**
   * name of the file uploaded
   */
  name: string;
  /**
   * The category of the company attachment.
   *
   * @remarks
   * - `gep_notice`: A tax notice attachment
   * - `compliance`: A compliance attachment
   * - `other`: Any other attachment type
   */
  category: CompanyAttachmentCategory;
  /**
   * The ISO 8601 timestamp of when an attachment was uploaded
   */
  uploadTime: string;
};

/** @internal */
export const CompanyAttachmentCategory$inboundSchema: z.ZodMiniType<
  CompanyAttachmentCategory,
  unknown
> = openEnums.inboundSchema(CompanyAttachmentCategory);

/** @internal */
export const CompanyAttachment$inboundSchema: z.ZodMiniType<
  CompanyAttachment,
  unknown
> = z.pipe(
  z.object({
    uuid: types.string(),
    name: types.string(),
    category: CompanyAttachmentCategory$inboundSchema,
    upload_time: types.string(),
  }),
  z.transform((v) => {
    return remap$(v, {
      "upload_time": "uploadTime",
    });
  }),
);

export function companyAttachmentFromJSON(
  jsonString: string,
): SafeParseResult<CompanyAttachment, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => CompanyAttachment$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'CompanyAttachment' from JSON`,
  );
}

/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../lib/primitives.js";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import * as types from "../types/primitives.js";
import { SDKValidationError } from "./errors/sdk-validation-error.js";

/**
 * A single pay period in a pay schedule preview, with check date, period boundaries, and payroll deadline.
 */
export type PaySchedulePreviewPayPeriod = {
  /**
   * The payment date, "Check date", for the pay period.
   */
  checkDate: Date;
  /**
   * The first day of the pay period.
   */
  startDate: Date;
  /**
   * The deadline to run payroll for direct deposit on the check date.
   */
  runPayrollBy: Date;
  /**
   * The last day of the pay period.
   */
  endDate: Date;
};

/** @internal */
export const PaySchedulePreviewPayPeriod$inboundSchema: z.ZodMiniType<
  PaySchedulePreviewPayPeriod,
  unknown
> = z.pipe(
  z.object({
    check_date: types.date(),
    start_date: types.date(),
    run_payroll_by: types.date(),
    end_date: types.date(),
  }),
  z.transform((v) => {
    return remap$(v, {
      "check_date": "checkDate",
      "start_date": "startDate",
      "run_payroll_by": "runPayrollBy",
      "end_date": "endDate",
    });
  }),
);

export function paySchedulePreviewPayPeriodFromJSON(
  jsonString: string,
): SafeParseResult<PaySchedulePreviewPayPeriod, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => PaySchedulePreviewPayPeriod$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'PaySchedulePreviewPayPeriod' from JSON`,
  );
}

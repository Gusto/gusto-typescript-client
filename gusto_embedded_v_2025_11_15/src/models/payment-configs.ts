/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../lib/primitives.js";
import { safeParse } from "../lib/schemas.js";
import * as openEnums from "../types/enums.js";
import { OpenEnum } from "../types/enums.js";
import { Result as SafeParseResult } from "../types/fp.js";
import * as types from "../types/primitives.js";
import { SDKValidationError } from "./errors/sdk-validation-error.js";

/**
 * Payment speed. READ-ONLY.
 *
 * @remarks
 * - `1-day`: Next-day ACH (only for partners that opt in).
 * - `2-day`: Two-day ACH.
 * - `4-day`: Standard ACH.
 */
export const PaymentConfigsPaymentSpeed = {
  OneMinusDay: "1-day",
  TwoMinusDay: "2-day",
  FourMinusDay: "4-day",
} as const;
/**
 * Payment speed. READ-ONLY.
 *
 * @remarks
 * - `1-day`: Next-day ACH (only for partners that opt in).
 * - `2-day`: Two-day ACH.
 * - `4-day`: Standard ACH.
 */
export type PaymentConfigsPaymentSpeed = OpenEnum<
  typeof PaymentConfigsPaymentSpeed
>;

/**
 * The type of blocker
 */
export const BlockerType = {
  MinimumDays: "minimum_days",
  MinimumFundedPayments: "minimum_funded_payments",
} as const;
/**
 * The type of blocker
 */
export type BlockerType = OpenEnum<typeof BlockerType>;

export type EarnedFastAchBlocker = {
  /**
   * The type of blocker
   */
  blockerType?: BlockerType | undefined;
  /**
   * The threshold needed to unblock
   */
  threshold?: number | undefined;
};

export type PaymentConfigs = {
  /**
   * Company uuid
   */
  companyUuid?: string | undefined;
  /**
   * Partner uuid
   */
  partnerUuid?: string | undefined;
  /**
   * Payment limit for 1-day or 2-day payroll (string representation of decimal).
   */
  fastPaymentLimit?: string | null | undefined;
  /**
   * Payment speed. READ-ONLY.
   *
   * @remarks
   * - `1-day`: Next-day ACH (only for partners that opt in).
   * - `2-day`: Two-day ACH.
   * - `4-day`: Standard ACH.
   */
  paymentSpeed?: PaymentConfigsPaymentSpeed | undefined;
  /**
   * Whether the company is configured to use the partner-owned disbursement payment rail
   */
  partnerOwnedDisbursement?: boolean | undefined;
  /**
   * Blockers preventing the company from earning fast ACH payments
   */
  earnedFastAchBlockers?: Array<EarnedFastAchBlocker> | undefined;
};

/** @internal */
export const PaymentConfigsPaymentSpeed$inboundSchema: z.ZodMiniType<
  PaymentConfigsPaymentSpeed,
  unknown
> = openEnums.inboundSchema(PaymentConfigsPaymentSpeed);

/** @internal */
export const BlockerType$inboundSchema: z.ZodMiniType<BlockerType, unknown> =
  openEnums.inboundSchema(BlockerType);

/** @internal */
export const EarnedFastAchBlocker$inboundSchema: z.ZodMiniType<
  EarnedFastAchBlocker,
  unknown
> = z.pipe(
  z.object({
    blocker_type: types.optional(BlockerType$inboundSchema),
    threshold: types.optional(types.number()),
  }),
  z.transform((v) => {
    return remap$(v, {
      "blocker_type": "blockerType",
    });
  }),
);

export function earnedFastAchBlockerFromJSON(
  jsonString: string,
): SafeParseResult<EarnedFastAchBlocker, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => EarnedFastAchBlocker$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'EarnedFastAchBlocker' from JSON`,
  );
}

/** @internal */
export const PaymentConfigs$inboundSchema: z.ZodMiniType<
  PaymentConfigs,
  unknown
> = z.pipe(
  z.object({
    company_uuid: types.optional(types.string()),
    partner_uuid: types.optional(types.string()),
    fast_payment_limit: z.optional(z.nullable(types.string())),
    payment_speed: types.optional(PaymentConfigsPaymentSpeed$inboundSchema),
    partner_owned_disbursement: types.optional(types.boolean()),
    earned_fast_ach_blockers: types.optional(
      z.array(z.lazy(() => EarnedFastAchBlocker$inboundSchema)),
    ),
  }),
  z.transform((v) => {
    return remap$(v, {
      "company_uuid": "companyUuid",
      "partner_uuid": "partnerUuid",
      "fast_payment_limit": "fastPaymentLimit",
      "payment_speed": "paymentSpeed",
      "partner_owned_disbursement": "partnerOwnedDisbursement",
      "earned_fast_ach_blockers": "earnedFastAchBlockers",
    });
  }),
);

export function paymentConfigsFromJSON(
  jsonString: string,
): SafeParseResult<PaymentConfigs, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => PaymentConfigs$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'PaymentConfigs' from JSON`,
  );
}

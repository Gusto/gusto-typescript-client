/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../lib/primitives.js";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import * as types from "../types/primitives.js";
import { SDKValidationError } from "./errors/sdk-validation-error.js";

export type RecurringReimbursement = {
  /**
   * The unique identifier of this recurring reimbursement.
   */
  uuid: string;
  /**
   * The UUID of the employee.
   */
  employeeUuid: string;
  /**
   * The description of the reimbursement.
   */
  description: string;
  /**
   * The dollar amount of the reimbursement.
   */
  amount: string;
  /**
   * The current version of the object. See the [versioning guide](https://docs.gusto.com/embedded-payroll/docs/idempotency) for information on how to use this field.
   */
  version: string;
  /**
   * The timestamp when this reimbursement was created.
   */
  createdAt?: string | undefined;
  /**
   * The timestamp when this reimbursement was last updated.
   */
  updatedAt?: string | undefined;
};

/** @internal */
export const RecurringReimbursement$inboundSchema: z.ZodMiniType<
  RecurringReimbursement,
  unknown
> = z.pipe(
  z.object({
    uuid: types.string(),
    employee_uuid: types.string(),
    description: types.string(),
    amount: types.string(),
    version: types.string(),
    created_at: types.optional(types.string()),
    updated_at: types.optional(types.string()),
  }),
  z.transform((v) => {
    return remap$(v, {
      "employee_uuid": "employeeUuid",
      "created_at": "createdAt",
      "updated_at": "updatedAt",
    });
  }),
);

export function recurringReimbursementFromJSON(
  jsonString: string,
): SafeParseResult<RecurringReimbursement, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => RecurringReimbursement$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'RecurringReimbursement' from JSON`,
  );
}

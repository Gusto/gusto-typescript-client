/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../lib/primitives.js";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import * as types from "../types/primitives.js";
import { SDKValidationError } from "./errors/sdk-validation-error.js";

/**
 * Object returned when creating a partner managed company
 */
export type PartnerManagedCompany = {
  /**
   * Access token that can be used for OAuth access to the account. Access tokens expire 2 hours after they are issued.
   */
  accessToken?: string | undefined;
  /**
   * Refresh token that can be exchanged for a new access token.
   */
  refreshToken?: string | undefined;
  /**
   * Gusto's UUID for the company
   */
  companyUuid?: string | undefined;
  /**
   * Time of access_token expiration in seconds
   */
  expiresIn?: number | undefined;
};

/** @internal */
export const PartnerManagedCompany$inboundSchema: z.ZodMiniType<
  PartnerManagedCompany,
  unknown
> = z.pipe(
  z.object({
    access_token: types.optional(types.string()),
    refresh_token: types.optional(types.string()),
    company_uuid: types.optional(types.string()),
    expires_in: types.optional(types.number()),
  }),
  z.transform((v) => {
    return remap$(v, {
      "access_token": "accessToken",
      "refresh_token": "refreshToken",
      "company_uuid": "companyUuid",
      "expires_in": "expiresIn",
    });
  }),
);

export function partnerManagedCompanyFromJSON(
  jsonString: string,
): SafeParseResult<PartnerManagedCompany, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => PartnerManagedCompany$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'PartnerManagedCompany' from JSON`,
  );
}

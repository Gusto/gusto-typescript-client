/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../lib/primitives.js";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import * as types from "../types/primitives.js";
import { SDKValidationError } from "./errors/sdk-validation-error.js";

export type CreateTokenAuthentication = {
  /**
   * A new access token that can be used for subsequent authenticated requests
   */
  accessToken: string;
  /**
   * The literal string 'Bearer'
   */
  tokenType: string;
  /**
   * The TTL of this token. After this amount of time, you must hit the refresh token endpoint to continue making authenticated requests.
   */
  expiresIn: number;
  /**
   * Datetime for when the new access token is created.
   */
  createdAt: number;
  /**
   * A token that must be passed to the refresh token endpoint to get a new authenticated token. Only present when refresh token is provided.
   */
  refreshToken?: string | null | undefined;
};

/** @internal */
export const CreateTokenAuthentication$inboundSchema: z.ZodMiniType<
  CreateTokenAuthentication,
  unknown
> = z.pipe(
  z.object({
    access_token: types.string(),
    token_type: z._default(types.string(), "Bearer"),
    expires_in: z._default(types.number(), 7200),
    created_at: types.number(),
    refresh_token: z.optional(z.nullable(types.string())),
  }),
  z.transform((v) => {
    return remap$(v, {
      "access_token": "accessToken",
      "token_type": "tokenType",
      "expires_in": "expiresIn",
      "created_at": "createdAt",
      "refresh_token": "refreshToken",
    });
  }),
);

export function createTokenAuthenticationFromJSON(
  jsonString: string,
): SafeParseResult<CreateTokenAuthentication, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => CreateTokenAuthentication$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'CreateTokenAuthentication' from JSON`,
  );
}

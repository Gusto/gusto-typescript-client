/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../lib/primitives.js";
import { safeParse } from "../lib/schemas.js";
import * as openEnums from "../types/enums.js";
import { OpenEnum } from "../types/enums.js";
import { Result as SafeParseResult } from "../types/fp.js";
import * as types from "../types/primitives.js";
import { SDKValidationError } from "./errors/sdk-validation-error.js";

/**
 * The resource associated with this access token. Null when
 *
 * @remarks
 * the token has no associated resource.
 */
export type TokenInfoResource = {
  /**
   * The type of resource associated with the access token, e.g. `Company` for a company-level token or `Oauth::Application` for a system-level token.
   *
   * @remarks
   */
  type?: string | undefined;
  /**
   * The UUID of the associated resource
   */
  uuid?: string | undefined;
};

/**
 * The type of resource owner:
 *
 * @remarks
 * - `CompanyAdmin`: A company administrator
 * - `Employee`: An employee
 * - `Contractor`: A contractor
 */
export const TokenInfoType = {
  CompanyAdmin: "CompanyAdmin",
  Employee: "Employee",
  Contractor: "Contractor",
} as const;
/**
 * The type of resource owner:
 *
 * @remarks
 * - `CompanyAdmin`: A company administrator
 * - `Employee`: An employee
 * - `Contractor`: A contractor
 */
export type TokenInfoType = OpenEnum<typeof TokenInfoType>;

/**
 * The resource owner (user) who authorized this access token. Null for
 *
 * @remarks
 * system-level tokens or when the owner cannot be determined.
 */
export type ResourceOwner = {
  /**
   * The type of resource owner:
   *
   * @remarks
   * - `CompanyAdmin`: A company administrator
   * - `Employee`: An employee
   * - `Contractor`: A contractor
   */
  type?: TokenInfoType | undefined;
  /**
   * The UUID of the resource owner
   */
  uuid?: string | undefined;
};

export type TokenInfo = {
  /**
   * Space-separated list of OAuth scopes granted to this access token.
   *
   * @remarks
   */
  scope?: string | undefined;
  /**
   * The resource associated with this access token. Null when
   *
   * @remarks
   * the token has no associated resource.
   */
  resource?: TokenInfoResource | null | undefined;
  /**
   * The resource owner (user) who authorized this access token. Null for
   *
   * @remarks
   * system-level tokens or when the owner cannot be determined.
   */
  resourceOwner?: ResourceOwner | null | undefined;
};

/** @internal */
export const TokenInfoResource$inboundSchema: z.ZodMiniType<
  TokenInfoResource,
  unknown
> = z.object({
  type: types.optional(types.string()),
  uuid: types.optional(types.string()),
});

export function tokenInfoResourceFromJSON(
  jsonString: string,
): SafeParseResult<TokenInfoResource, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => TokenInfoResource$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'TokenInfoResource' from JSON`,
  );
}

/** @internal */
export const TokenInfoType$inboundSchema: z.ZodMiniType<
  TokenInfoType,
  unknown
> = openEnums.inboundSchema(TokenInfoType);

/** @internal */
export const ResourceOwner$inboundSchema: z.ZodMiniType<
  ResourceOwner,
  unknown
> = z.object({
  type: types.optional(TokenInfoType$inboundSchema),
  uuid: types.optional(types.string()),
});

export function resourceOwnerFromJSON(
  jsonString: string,
): SafeParseResult<ResourceOwner, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ResourceOwner$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ResourceOwner' from JSON`,
  );
}

/** @internal */
export const TokenInfo$inboundSchema: z.ZodMiniType<TokenInfo, unknown> = z
  .pipe(
    z.object({
      scope: types.optional(types.string()),
      resource: z.optional(
        z.nullable(z.lazy(() => TokenInfoResource$inboundSchema)),
      ),
      resource_owner: z.optional(
        z.nullable(z.lazy(() => ResourceOwner$inboundSchema)),
      ),
    }),
    z.transform((v) => {
      return remap$(v, {
        "resource_owner": "resourceOwner",
      });
    }),
  );

export function tokenInfoFromJSON(
  jsonString: string,
): SafeParseResult<TokenInfo, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => TokenInfo$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'TokenInfo' from JSON`,
  );
}

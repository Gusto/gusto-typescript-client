/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../lib/primitives.js";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import * as types from "../types/primitives.js";
import { SDKValidationError } from "./errors/sdk-validation-error.js";

export type TaxSuggestion = {
  /**
   * The ID of the tax.
   */
  taxId?: number | undefined;
  /**
   * Calculated tax amount.
   */
  amount?: string | undefined;
};

/**
 * The representation of an external payroll with minimal information.
 */
export type ExternalPayrollTaxSuggestions = {
  /**
   * The UUID of the employee.
   */
  employeeUuid?: string | undefined;
  /**
   * Possible tax liabilities selections.
   */
  taxSuggestions?: Array<TaxSuggestion> | undefined;
};

/** @internal */
export const TaxSuggestion$inboundSchema: z.ZodMiniType<
  TaxSuggestion,
  unknown
> = z.pipe(
  z.object({
    tax_id: types.optional(types.number()),
    amount: types.optional(types.string()),
  }),
  z.transform((v) => {
    return remap$(v, {
      "tax_id": "taxId",
    });
  }),
);

export function taxSuggestionFromJSON(
  jsonString: string,
): SafeParseResult<TaxSuggestion, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => TaxSuggestion$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'TaxSuggestion' from JSON`,
  );
}

/** @internal */
export const ExternalPayrollTaxSuggestions$inboundSchema: z.ZodMiniType<
  ExternalPayrollTaxSuggestions,
  unknown
> = z.pipe(
  z.object({
    employee_uuid: types.optional(types.string()),
    tax_suggestions: types.optional(
      z.array(z.lazy(() => TaxSuggestion$inboundSchema)),
    ),
  }),
  z.transform((v) => {
    return remap$(v, {
      "employee_uuid": "employeeUuid",
      "tax_suggestions": "taxSuggestions",
    });
  }),
);

export function externalPayrollTaxSuggestionsFromJSON(
  jsonString: string,
): SafeParseResult<ExternalPayrollTaxSuggestions, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ExternalPayrollTaxSuggestions$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ExternalPayrollTaxSuggestions' from JSON`,
  );
}

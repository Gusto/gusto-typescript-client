/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../lib/primitives.js";

/**
 * The request body for creating an external payroll.
 */
export type ExternalPayrollCreateRequest = {
  /**
   * The check date of the external payroll.
   */
  checkDate: Date;
  /**
   * The start date of the external payroll payment period.
   */
  paymentPeriodStartDate: Date;
  /**
   * The end date of the external payroll payment period.
   */
  paymentPeriodEndDate: Date;
};

/** @internal */
export type ExternalPayrollCreateRequest$Outbound = {
  check_date: string;
  payment_period_start_date: string;
  payment_period_end_date: string;
};

/** @internal */
export const ExternalPayrollCreateRequest$outboundSchema: z.ZodMiniType<
  ExternalPayrollCreateRequest$Outbound,
  ExternalPayrollCreateRequest
> = z.pipe(
  z.object({
    checkDate: z.pipe(
      z.date(),
      z.transform(v => v.toISOString().slice(0, "YYYY-MM-DD".length)),
    ),
    paymentPeriodStartDate: z.pipe(
      z.date(),
      z.transform(v => v.toISOString().slice(0, "YYYY-MM-DD".length)),
    ),
    paymentPeriodEndDate: z.pipe(
      z.date(),
      z.transform(v => v.toISOString().slice(0, "YYYY-MM-DD".length)),
    ),
  }),
  z.transform((v) => {
    return remap$(v, {
      checkDate: "check_date",
      paymentPeriodStartDate: "payment_period_start_date",
      paymentPeriodEndDate: "payment_period_end_date",
    });
  }),
);

export function externalPayrollCreateRequestToJSON(
  externalPayrollCreateRequest: ExternalPayrollCreateRequest,
): string {
  return JSON.stringify(
    ExternalPayrollCreateRequest$outboundSchema.parse(
      externalPayrollCreateRequest,
    ),
  );
}

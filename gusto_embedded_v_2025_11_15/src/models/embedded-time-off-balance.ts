/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../lib/primitives.js";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import * as types from "../types/primitives.js";
import { SDKValidationError } from "./errors/sdk-validation-error.js";

export type Balance = {
  /**
   * The UUID of the time off policy.
   */
  policyUuid?: string | undefined;
  /**
   * The employee's current available balance hours for this policy.
   */
  balanceHours?: string | undefined;
  /**
   * The total hours accrued year-to-date for this policy.
   */
  accruedHours?: string | undefined;
  /**
   * The total hours used year-to-date for this policy.
   */
  usedHours?: string | undefined;
  /**
   * The total hours from pending time off requests for this policy.
   */
  pendingHours?: string | null | undefined;
};

/**
 * Time off balance for an employee, grouped by policy.
 */
export type EmbeddedTimeOffBalance = {
  /**
   * The UUID of the employee.
   */
  employeeUuid?: string | undefined;
  /**
   * The employee's time off balances, one entry per policy.
   */
  balances?: Array<Balance> | undefined;
};

/** @internal */
export const Balance$inboundSchema: z.ZodMiniType<Balance, unknown> = z.pipe(
  z.object({
    policy_uuid: types.optional(types.string()),
    balance_hours: types.optional(types.string()),
    accrued_hours: types.optional(types.string()),
    used_hours: types.optional(types.string()),
    pending_hours: z.optional(z.nullable(types.string())),
  }),
  z.transform((v) => {
    return remap$(v, {
      "policy_uuid": "policyUuid",
      "balance_hours": "balanceHours",
      "accrued_hours": "accruedHours",
      "used_hours": "usedHours",
      "pending_hours": "pendingHours",
    });
  }),
);

export function balanceFromJSON(
  jsonString: string,
): SafeParseResult<Balance, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => Balance$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'Balance' from JSON`,
  );
}

/** @internal */
export const EmbeddedTimeOffBalance$inboundSchema: z.ZodMiniType<
  EmbeddedTimeOffBalance,
  unknown
> = z.pipe(
  z.object({
    employee_uuid: types.optional(types.string()),
    balances: types.optional(z.array(z.lazy(() => Balance$inboundSchema))),
  }),
  z.transform((v) => {
    return remap$(v, {
      "employee_uuid": "employeeUuid",
    });
  }),
);

export function embeddedTimeOffBalanceFromJSON(
  jsonString: string,
): SafeParseResult<EmbeddedTimeOffBalance, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => EmbeddedTimeOffBalance$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'EmbeddedTimeOffBalance' from JSON`,
  );
}

/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../lib/primitives.js";
import { safeParse } from "../lib/schemas.js";
import * as openEnums from "../types/enums.js";
import { OpenEnum } from "../types/enums.js";
import { Result as SafeParseResult } from "../types/fp.js";
import * as types from "../types/primitives.js";
import { SDKValidationError } from "./errors/sdk-validation-error.js";

export type PayrollSubmissionBlockerTypeUnblockOption = {
  /**
   * The type of unblock option for the submission blocker.
   */
  unblockType?: string | undefined;
  /**
   * The payment check date associated with the unblock option.
   */
  checkDate?: string | undefined;
  /**
   * Additional data associated with the unblock option.
   */
  metadata?: { [k: string]: any } | undefined;
};

/**
 * The status of the submission blocker.
 */
export const PayrollSubmissionBlockerTypeStatus = {
  Unresolved: "unresolved",
  Resolved: "resolved",
} as const;
/**
 * The status of the submission blocker.
 */
export type PayrollSubmissionBlockerTypeStatus = OpenEnum<
  typeof PayrollSubmissionBlockerTypeStatus
>;

/**
 * A blocker that prevents payment submission.
 */
export type PayrollSubmissionBlockerType = {
  /**
   * The type of blocker that's blocking the payment submission.
   */
  blockerType?: string | undefined;
  /**
   * The name of the submission blocker.
   */
  blockerName?: string | undefined;
  /**
   * The available options to unblock a submission blocker.
   */
  unblockOptions?: Array<PayrollSubmissionBlockerTypeUnblockOption> | undefined;
  /**
   * The unblock option that's been selected to resolve the submission blocker.
   */
  selectedOption?: string | null | undefined;
  /**
   * The status of the submission blocker.
   */
  status?: PayrollSubmissionBlockerTypeStatus | undefined;
};

/** @internal */
export const PayrollSubmissionBlockerTypeUnblockOption$inboundSchema:
  z.ZodMiniType<PayrollSubmissionBlockerTypeUnblockOption, unknown> = z.pipe(
    z.object({
      unblock_type: types.optional(types.string()),
      check_date: types.optional(types.string()),
      metadata: types.optional(z.record(z.string(), z.any())),
    }),
    z.transform((v) => {
      return remap$(v, {
        "unblock_type": "unblockType",
        "check_date": "checkDate",
      });
    }),
  );

export function payrollSubmissionBlockerTypeUnblockOptionFromJSON(
  jsonString: string,
): SafeParseResult<
  PayrollSubmissionBlockerTypeUnblockOption,
  SDKValidationError
> {
  return safeParse(
    jsonString,
    (x) =>
      PayrollSubmissionBlockerTypeUnblockOption$inboundSchema.parse(
        JSON.parse(x),
      ),
    `Failed to parse 'PayrollSubmissionBlockerTypeUnblockOption' from JSON`,
  );
}

/** @internal */
export const PayrollSubmissionBlockerTypeStatus$inboundSchema: z.ZodMiniType<
  PayrollSubmissionBlockerTypeStatus,
  unknown
> = openEnums.inboundSchema(PayrollSubmissionBlockerTypeStatus);

/** @internal */
export const PayrollSubmissionBlockerType$inboundSchema: z.ZodMiniType<
  PayrollSubmissionBlockerType,
  unknown
> = z.pipe(
  z.object({
    blocker_type: types.optional(types.string()),
    blocker_name: types.optional(types.string()),
    unblock_options: types.optional(z.array(z.lazy(() =>
      PayrollSubmissionBlockerTypeUnblockOption$inboundSchema
    ))),
    selected_option: z.optional(z.nullable(types.string())),
    status: types.optional(PayrollSubmissionBlockerTypeStatus$inboundSchema),
  }),
  z.transform((v) => {
    return remap$(v, {
      "blocker_type": "blockerType",
      "blocker_name": "blockerName",
      "unblock_options": "unblockOptions",
      "selected_option": "selectedOption",
    });
  }),
);

export function payrollSubmissionBlockerTypeFromJSON(
  jsonString: string,
): SafeParseResult<PayrollSubmissionBlockerType, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => PayrollSubmissionBlockerType$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'PayrollSubmissionBlockerType' from JSON`,
  );
}

/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { companyFormsSign } from "../../funcs/companyFormsSign.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.PutV1CompanyFormSignRequest$inboundSchema,
};

export const tool$companyFormsSign: ToolDefinition<typeof args> = {
  name: "company-forms_sign",
  description: `Sign a company form

Sign a company form. Company forms must be signed by the company signatory.

scope: \`company_forms:sign\``,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await companyFormsSign(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};

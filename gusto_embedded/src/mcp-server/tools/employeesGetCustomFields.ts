/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { employeesGetCustomFields } from "../../funcs/employeesGetCustomFields.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetV1EmployeesEmployeeIdCustomFieldsRequest$inboundSchema,
};

export const tool$employeesGetCustomFields: ToolDefinition<typeof args> = {
  name: "employees_get-custom-fields",
  description: `Get an employee's custom fields

Returns a list of the employee's custom fields.

scope: \`employees:read\``,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await employeesGetCustomFields(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};

/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { achTransactionsGetAll } from "../../funcs/achTransactionsGetAll.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetAchTransactionsRequest$inboundSchema,
};

export const tool$achTransactionsGetAll: ToolDefinition<typeof args> = {
  name: "ach-transactions_get-all",
  description: `Get all ACH transactions for a company

Fetches all ACH transactions for a company.

scope: \`ach_transactions:read\``,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await achTransactionsGetAll(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};

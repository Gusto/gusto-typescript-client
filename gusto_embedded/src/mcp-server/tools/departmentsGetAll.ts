/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { departmentsGetAll } from "../../funcs/departmentsGetAll.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetCompaniesDepartmentsRequest$inboundSchema,
};

export const tool$departmentsGetAll: ToolDefinition<typeof args> = {
  name: "departments_get-all",
  description: `Get all departments of a company

Get all of the departments for a given company with the employees and contractors assigned to that department.

scope: \`departments:read\``,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await departmentsGetAll(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};

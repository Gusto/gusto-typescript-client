/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { employeeEmploymentsGetHistory } from "../../funcs/employeeEmploymentsGetHistory.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request:
    operations.GetV1EmployeesEmployeeIdEmploymentHistoryRequest$inboundSchema,
};

export const tool$employeeEmploymentsGetHistory: ToolDefinition<typeof args> = {
  name: "employee-employments_get-history",
  description: `Get employment history for an employee

Retrieve the employment history for a given employee, which includes termination and rehire.

scope: \`employments:read\``,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await employeeEmploymentsGetHistory(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};

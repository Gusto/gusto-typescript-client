/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { timeOffPoliciesUpdateBalance } from "../../funcs/timeOffPoliciesUpdateBalance.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request:
    operations
      .PutVersionTimeOffPoliciesTimeOffPolicyUuidBalanceRequest$inboundSchema,
};

export const tool$timeOffPoliciesUpdateBalance: ToolDefinition<typeof args> = {
  name: "time-off-policies_update-balance",
  description: `Update employee time off hour balances

Updates time off hours balances for employees for a time off policy

scope: \`time_off_policies:write\``,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await timeOffPoliciesUpdateBalance(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};

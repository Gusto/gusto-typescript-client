/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { contractorFormsGetPdf } from "../../funcs/contractorFormsGetPdf.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetV1ContractorFormPdfRequest$inboundSchema,
};

export const tool$contractorFormsGetPdf: ToolDefinition<typeof args> = {
  name: "contractor-forms_get-pdf",
  description: `Get the contractor form pdf

Get the link to the form PDF

scope: \`contractor_forms:read\``,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await contractorFormsGetPdf(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};

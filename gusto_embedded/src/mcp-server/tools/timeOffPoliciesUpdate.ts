/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { timeOffPoliciesUpdate } from "../../funcs/timeOffPoliciesUpdate.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.PutTimeOffPoliciesTimeOffPolicyUuidRequest$inboundSchema,
};

export const tool$timeOffPoliciesUpdate: ToolDefinition<typeof args> = {
  name: "time-off-policies_update",
  description: `Update a time off policy

Update a time off policy

scope: \`time_off_policies:write\``,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await timeOffPoliciesUpdate(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};

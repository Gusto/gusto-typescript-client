/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { earningTypesUpdate } from "../../funcs/earningTypesUpdate.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request:
    operations
      .PutV1CompaniesCompanyIdEarningTypesEarningTypeUuidRequest$inboundSchema,
};

export const tool$earningTypesUpdate: ToolDefinition<typeof args> = {
  name: "earning-types_update",
  description: `Update an earning type

Update an earning type.

scope: \`payrolls:write\``,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await earningTypesUpdate(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};

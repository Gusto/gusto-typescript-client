/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { companiesAcceptTermsOfService } from "../../funcs/companiesAcceptTermsOfService.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request:
    operations
      .PostPartnerManagedCompaniesCompanyUuidAcceptTermsOfServiceRequest$inboundSchema,
};

export const tool$companiesAcceptTermsOfService: ToolDefinition<typeof args> = {
  name: "companies_accept-terms-of-service",
  description: `Accept terms of service for a company user

Accept the Gusto Embedded Payroll's [Terms of Service](https://flows.gusto.com/terms).
The user must have a role in the company in order to accept the Terms of Service.

scope: \`terms_of_services:write\``,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await companiesAcceptTermsOfService(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};

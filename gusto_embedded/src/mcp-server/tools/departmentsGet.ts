/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { departmentsGet } from "../../funcs/departmentsGet.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetDepartmentRequest$inboundSchema,
};

export const tool$departmentsGet: ToolDefinition<typeof args> = {
  name: "departments_get",
  description: `Get a department

Get a department given the UUID

scope: \`departments:read\`
`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await departmentsGet(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};

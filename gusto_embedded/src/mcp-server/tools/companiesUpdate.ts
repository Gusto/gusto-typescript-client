/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { companiesUpdate } from "../../funcs/companiesUpdate.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.PutV1CompaniesRequest$inboundSchema,
};

export const tool$companiesUpdate: ToolDefinition<typeof args> = {
  name: "companies_update",
  description: `Update a company

Update a company.

scope: \`companies:write\``,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await companiesUpdate(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};

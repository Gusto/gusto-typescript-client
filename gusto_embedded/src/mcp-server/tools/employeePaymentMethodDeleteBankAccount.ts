/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { employeePaymentMethodDeleteBankAccount } from "../../funcs/employeePaymentMethodDeleteBankAccount.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request:
    operations
      .DeleteV1EmployeesEmployeeIdBankAccountsBankAccountIdRequest$inboundSchema,
};

export const tool$employeePaymentMethodDeleteBankAccount: ToolDefinition<
  typeof args
> = {
  name: "employee-payment-method_delete-bank-account",
  description: `Delete an employee bank account

Deletes an employee bank account. To update an employee's bank
account details, delete the bank account first and create a new one.

scope: \`employee_payment_methods:write\``,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await employeePaymentMethodDeleteBankAccount(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(void 0, apiCall);
  },
};

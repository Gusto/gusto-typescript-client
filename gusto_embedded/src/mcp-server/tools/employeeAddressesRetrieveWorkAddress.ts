/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { employeeAddressesRetrieveWorkAddress } from "../../funcs/employeeAddressesRetrieveWorkAddress.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetV1WorkAddressesWorkAddressUuidRequest$inboundSchema,
};

export const tool$employeeAddressesRetrieveWorkAddress: ToolDefinition<
  typeof args
> = {
  name: "employee-addresses_retrieve-work-address",
  description: `Get an employee work address

The work address of an employee is used for payroll tax purposes.

scope: \`employees:read\``,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await employeeAddressesRetrieveWorkAddress(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};

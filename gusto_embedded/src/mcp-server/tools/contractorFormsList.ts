/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { contractorFormsList } from "../../funcs/contractorFormsList.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetV1ContractorFormsRequest$inboundSchema,
};

export const tool$contractorFormsList: ToolDefinition<typeof args> = {
  name: "contractor-forms_list",
  description: `Get all contractor forms

Get a list of all contractor's forms

scope: \`contractor_forms:read\``,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await contractorFormsList(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};

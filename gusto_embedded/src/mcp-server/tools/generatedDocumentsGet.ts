/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { generatedDocumentsGet } from "../../funcs/generatedDocumentsGet.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request:
    operations
      .GetV1GeneratedDocumentsDocumentTypeRequestUuidRequest$inboundSchema,
};

export const tool$generatedDocumentsGet: ToolDefinition<typeof args> = {
  name: "generated-documents_get",
  description: `Get a generated document

Get a document given the request_uuid. The response will include the generation request's status and urls to the document. A list of urls is returned as certain document types require several urls.

scope: \`generated_documents:read\``,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await generatedDocumentsGet(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};

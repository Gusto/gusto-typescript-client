/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { earningTypesCreate } from "../../funcs/earningTypesCreate.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.PostV1CompaniesCompanyIdEarningTypesRequest$inboundSchema,
};

export const tool$earningTypesCreate: ToolDefinition<typeof args> = {
  name: "earning-types_create",
  description: `Create a custom earning type

Create a custom earning type.

If an inactive earning type exists with the same name, this will reactivate it instead of creating a new one.

scope: \`payrolls:write\``,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await earningTypesCreate(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};

/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { employeesGetTimeOffActivities } from "../../funcs/employeesGetTimeOffActivities.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetVersionEmployeesTimeOffActivitiesRequest$inboundSchema,
};

export const tool$employeesGetTimeOffActivities: ToolDefinition<typeof args> = {
  name: "employees_get-time-off-activities",
  description: `Get employee time off activities

Get employee time off activities.

scope: \`employee_time_off_activities:read\``,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await employeesGetTimeOffActivities(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};

/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { timeOffPoliciesDeactivate } from "../../funcs/timeOffPoliciesDeactivate.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request:
    operations
      .PutV1TimeOffPoliciesTimeOffPolicyUuidDeactivateRequest$inboundSchema,
};

export const tool$timeOffPoliciesDeactivate: ToolDefinition<typeof args> = {
  name: "time-off-policies_deactivate",
  description: `Deactivate a time off policy

Deactivate a time off policy

scope: \`time_off_policies:write\``,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await timeOffPoliciesDeactivate(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};

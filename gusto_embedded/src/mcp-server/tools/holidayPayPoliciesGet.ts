/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { holidayPayPoliciesGet } from "../../funcs/holidayPayPoliciesGet.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request:
    operations.GetCompaniesCompanyUuidHolidayPayPolicyRequest$inboundSchema,
};

export const tool$holidayPayPoliciesGet: ToolDefinition<typeof args> = {
  name: "holiday-pay-policies_get",
  description: `Get a company's holiday pay policy

Get a company's holiday pay policy

scope: \`holiday_pay_policies:read\``,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await holidayPayPoliciesGet(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};

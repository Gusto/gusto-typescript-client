/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { reportsGet } from "../../funcs/reportsGet.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetReportsReportUuidRequest$inboundSchema,
};

export const tool$reportsGet: ToolDefinition<typeof args> = {
  name: "reports_get",
  description: `Get a report

Get a company's report given the \`request_uuid\`. The response will include the report request's status and, if complete, the report URL.

scope: \`company_reports:read\``,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await reportsGet(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};

/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { employeeAddressesDeleteWorkAddress } from "../../funcs/employeeAddressesDeleteWorkAddress.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.DeleteV1WorkAddressesWorkAddressUuidRequest$inboundSchema,
};

export const tool$employeeAddressesDeleteWorkAddress: ToolDefinition<
  typeof args
> = {
  name: "employee-addresses_delete-work-address",
  description: `Delete an employee's work address

Used for deleting an employee's work address.  Cannot delete the employee's active work address.

scope: \`employees:manage\``,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await employeeAddressesDeleteWorkAddress(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(void 0, apiCall);
  },
};

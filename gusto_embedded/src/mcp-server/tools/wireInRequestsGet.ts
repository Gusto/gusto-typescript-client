/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { wireInRequestsGet } from "../../funcs/wireInRequestsGet.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetWireInRequestsWireInRequestUuidRequest$inboundSchema,
};

export const tool$wireInRequestsGet: ToolDefinition<typeof args> = {
  name: "wire-in-requests_get",
  description: `Get a single Wire In Request

Fetch a Wire In Request.

scope: \`payrolls:read\``,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await wireInRequestsGet(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};

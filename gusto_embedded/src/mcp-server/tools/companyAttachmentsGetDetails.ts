/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { companyAttachmentsGetDetails } from "../../funcs/companyAttachmentsGetDetails.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetV1CompaniesAttachmentRequest$inboundSchema,
};

export const tool$companyAttachmentsGetDetails: ToolDefinition<typeof args> = {
  name: "company-attachments_get-details",
  description: `Get Company Attachment Details

Retrieve the detail of an attachment uploaded by the company.

scope: \`company_attachments:read\``,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await companyAttachmentsGetDetails(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};

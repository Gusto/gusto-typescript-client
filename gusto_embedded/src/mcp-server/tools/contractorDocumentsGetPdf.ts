/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { contractorDocumentsGetPdf } from "../../funcs/contractorDocumentsGetPdf.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetV1ContractorDocumentPdfRequest$inboundSchema,
};

export const tool$contractorDocumentsGetPdf: ToolDefinition<typeof args> = {
  name: "contractor-documents_get-pdf",
  description: `Get the contractor document pdf

Get the contractor document pdf.

scope: \`contractor_documents:read\``,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await contractorDocumentsGetPdf(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};

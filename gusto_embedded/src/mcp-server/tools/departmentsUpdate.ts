/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { departmentsUpdate } from "../../funcs/departmentsUpdate.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.PutDepartmentsRequest$inboundSchema,
};

export const tool$departmentsUpdate: ToolDefinition<typeof args> = {
  name: "departments_update",
  description: `Update a department

Update a department

scope: \`departments:write\``,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await departmentsUpdate(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};

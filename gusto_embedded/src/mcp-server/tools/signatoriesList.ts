/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { signatoriesList } from "../../funcs/signatoriesList.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetV1CompaniesCompanyUuidSignatoriesRequest$inboundSchema,
};

export const tool$signatoriesList: ToolDefinition<typeof args> = {
  name: "signatories_list",
  description: `Get all company signatories

Returns company signatories. Currently we only support a single signatory per company.

scope: \`signatories:read\``,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await signatoriesList(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};

/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { locationsGetMinimumWages } from "../../funcs/locationsGetMinimumWages.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request:
    operations.GetV1LocationsLocationUuidMinimumWagesRequest$inboundSchema,
};

export const tool$locationsGetMinimumWages: ToolDefinition<typeof args> = {
  name: "locations_get-minimum-wages",
  description: `Get minimum wages for a location

Get minimum wages for a location

scope: \`companies:read\``,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await locationsGetMinimumWages(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};

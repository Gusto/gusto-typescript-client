/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { paySchedulesPreviewAssignment } from "../../funcs/paySchedulesPreviewAssignment.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request:
    operations
      .PostV1CompaniesCompanyIdPaySchedulesAssignmentPreviewRequest$inboundSchema,
};

export const tool$paySchedulesPreviewAssignment: ToolDefinition<typeof args> = {
  name: "pay-schedules_preview-assignment",
  description: `Preview pay schedule assignments for a company

This endpoint returns the employee changes, including pay period and transition pay periods, for changing the pay schedule.

scope: \`pay_schedules:write\``,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await paySchedulesPreviewAssignment(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};

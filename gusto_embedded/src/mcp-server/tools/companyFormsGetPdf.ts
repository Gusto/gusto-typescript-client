/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { companyFormsGetPdf } from "../../funcs/companyFormsGetPdf.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetV1CompanyFormPdfRequest$inboundSchema,
};

export const tool$companyFormsGetPdf: ToolDefinition<typeof args> = {
  name: "company-forms_get-pdf",
  description: `Get a company form pdf

Get the link to the form PDF

scope: \`company_forms:read\``,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await companyFormsGetPdf(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};

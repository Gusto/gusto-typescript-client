/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { companiesListAdmins } from "../../funcs/companiesListAdmins.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetV1CompaniesCompanyIdAdminsRequest$inboundSchema,
};

export const tool$companiesListAdmins: ToolDefinition<typeof args> = {
  name: "companies_list-admins",
  description: `Get all the admins at a company

Returns a list of all the admins at a company

scope: \`company_admin:read\``,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await companiesListAdmins(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};

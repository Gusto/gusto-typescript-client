/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { employeeBenefitsDelete } from "../../funcs/employeeBenefitsDelete.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request:
    operations.DeleteV1EmployeeBenefitsEmployeeBenefitIdRequest$inboundSchema,
};

export const tool$employeeBenefitsDelete: ToolDefinition<typeof args> = {
  name: "employee-benefits_delete",
  description: `Delete an employee benefit

Employee benefits represent an employee enrolled in a particular company benefit. It includes information specific to that employee’s enrollment.

scope: \`employee_benefits:write\``,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await employeeBenefitsDelete(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(void 0, apiCall);
  },
};

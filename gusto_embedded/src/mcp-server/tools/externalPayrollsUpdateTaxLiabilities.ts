/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { externalPayrollsUpdateTaxLiabilities } from "../../funcs/externalPayrollsUpdateTaxLiabilities.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.PutV1TaxLiabilitiesRequest$inboundSchema,
};

export const tool$externalPayrollsUpdateTaxLiabilities: ToolDefinition<
  typeof args
> = {
  name: "external-payrolls_update-tax-liabilities",
  description: `Update tax liabilities

Update tax liabilities for a company.

scope: \`external_payrolls:write\``,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await externalPayrollsUpdateTaxLiabilities(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};

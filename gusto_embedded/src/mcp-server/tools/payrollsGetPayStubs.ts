/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { payrollsGetPayStubs } from "../../funcs/payrollsGetPayStubs.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetV1EmployeesEmployeeUuidPayStubsRequest$inboundSchema,
};

export const tool$payrollsGetPayStubs: ToolDefinition<typeof args> = {
  name: "payrolls_get-pay-stubs",
  description: `Get an employee's pay stubs

Get an employee's pay stubs

scope: \`pay_stubs:read\``,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await payrollsGetPayStubs(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};

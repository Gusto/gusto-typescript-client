/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { holidayPayPoliciesDelete } from "../../funcs/holidayPayPoliciesDelete.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request:
    operations.DeleteCompaniesCompanyUuidHolidayPayPolicyRequest$inboundSchema,
};

export const tool$holidayPayPoliciesDelete: ToolDefinition<typeof args> = {
  name: "holiday-pay-policies_delete",
  description: `Delete a company's holiday pay policy

Delete a company's holiday pay policy

scope: \`holiday_pay_policies:write\``,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await holidayPayPoliciesDelete(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(void 0, apiCall);
  },
};

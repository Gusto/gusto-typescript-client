/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { externalPayrollsFinalizeTaxLiabilities } from "../../funcs/externalPayrollsFinalizeTaxLiabilities.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.PutV1TaxLiabilitiesFinishRequest$inboundSchema,
};

export const tool$externalPayrollsFinalizeTaxLiabilities: ToolDefinition<
  typeof args
> = {
  name: "external-payrolls_finalize-tax-liabilities",
  description:
    `Finalize tax liabilities options and convert into processed payrolls

Finalizes tax liabilities for a company. All external payrolls edit action will be disabled.

scope: \`external_payrolls:write\``,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await externalPayrollsFinalizeTaxLiabilities(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(void 0, apiCall);
  },
};

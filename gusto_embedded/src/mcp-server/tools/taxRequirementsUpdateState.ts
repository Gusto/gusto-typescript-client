/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { taxRequirementsUpdateState } from "../../funcs/taxRequirementsUpdateState.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request:
    operations
      .PutV1CompaniesCompanyUuidTaxRequirementsStateRequest$inboundSchema,
};

export const tool$taxRequirementsUpdateState: ToolDefinition<typeof args> = {
  name: "tax-requirements_update-state",
  description: `Update State Tax Requirements

Update State Tax Requirements

scope: \`company_tax_requirements:write\``,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await taxRequirementsUpdateState(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(void 0, apiCall);
  },
};

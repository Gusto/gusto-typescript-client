/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { introspectionGetInfo } from "../../funcs/introspectionGetInfo.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetV1TokenInfoRequest$inboundSchema,
};

export const tool$introspectionGetInfo: ToolDefinition<typeof args> = {
  name: "introspection_get-info",
  description: `Get info about the current access token

Returns scope and resource information associated with the current access token.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await introspectionGetInfo(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};

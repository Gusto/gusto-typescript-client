/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { i9VerificationEmployerSign } from "../../funcs/i9VerificationEmployerSign.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request:
    operations
      .PutV1EmployeesEmployeeIdI9AuthorizationEmployerSignRequest$inboundSchema,
};

export const tool$i9VerificationEmployerSign: ToolDefinition<typeof args> = {
  name: "i9-verification_employer-sign",
  description: `Employer sign an employee's Form I-9

Sign an employee's Form I-9 as an employer. Once the form is signed, the employee's I-9 authorization is considered complete and cannot be modified.

scope: \`i9_authorizations:manage\``,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await i9VerificationEmployerSign(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};

/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { contractorPaymentsList } from "../../funcs/contractorPaymentsList.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request:
    operations.GetV1CompaniesCompanyIdContractorPaymentsRequest$inboundSchema,
};

export const tool$contractorPaymentsList: ToolDefinition<typeof args> = {
  name: "contractor-payments_list",
  description: `Get contractor payments for a company

Returns an object containing individual contractor payments, within a given time period, including totals.

scope: \`payrolls:read\``,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await contractorPaymentsList(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};

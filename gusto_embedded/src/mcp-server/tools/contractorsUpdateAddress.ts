/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { contractorsUpdateAddress } from "../../funcs/contractorsUpdateAddress.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request:
    operations.PutV1ContractorsContractorUuidAddressRequest$inboundSchema,
};

export const tool$contractorsUpdateAddress: ToolDefinition<typeof args> = {
  name: "contractors_update-address",
  description: `Update a contractor's address

The address of a contractor is used to determine certain tax information about them. Addresses are geocoded on create and update to ensure validity.

scope: \`contractors:write\``,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await contractorsUpdateAddress(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};

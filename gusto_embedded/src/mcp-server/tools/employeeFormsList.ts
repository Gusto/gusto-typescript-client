/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { employeeFormsList } from "../../funcs/employeeFormsList.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetV1EmployeeFormsRequest$inboundSchema,
};

export const tool$employeeFormsList: ToolDefinition<typeof args> = {
  name: "employee-forms_list",
  description: `Get all employee forms

Get a list of all employee's forms

scope: \`employee_forms:read\``,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await employeeFormsList(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};

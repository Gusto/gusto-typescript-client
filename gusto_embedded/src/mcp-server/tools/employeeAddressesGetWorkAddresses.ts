/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { employeeAddressesGetWorkAddresses } from "../../funcs/employeeAddressesGetWorkAddresses.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request:
    operations.GetV1EmployeesEmployeeIdWorkAddressesRequest$inboundSchema,
};

export const tool$employeeAddressesGetWorkAddresses: ToolDefinition<
  typeof args
> = {
  name: "employee-addresses_get-work-addresses",
  description: `Get an employee's work addresses

Returns a list of an employee's work addresses. Each address includes its effective date and a boolean
signifying if it is the currently active work address.

scope: \`employees:read\``,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await employeeAddressesGetWorkAddresses(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};

/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { contractorPaymentMethodsCreateBankAccount } from "../../funcs/contractorPaymentMethodsCreateBankAccount.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request:
    operations.PostV1ContractorsContractorUuidBankAccountsRequest$inboundSchema,
};

export const tool$contractorPaymentMethodsCreateBankAccount: ToolDefinition<
  typeof args
> = {
  name: "contractor-payment-methods_create-bank-account",
  description: `Create a contractor bank account

Creates a contractor bank account.

Note: We currently only support one bank account per contractor. Using this endpoint on a contractor who already has a bank account will just replace it.

scope: \`contractor_payment_methods:write\``,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await contractorPaymentMethodsCreateBankAccount(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};

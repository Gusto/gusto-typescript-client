/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { employeesUpdateOnboardingDocumentsConfig } from "../../funcs/employeesUpdateOnboardingDocumentsConfig.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request:
    operations
      .PutV1EmployeesEmployeeIdOnboardingDocumentsConfigRequest$inboundSchema,
};

export const tool$employeesUpdateOnboardingDocumentsConfig: ToolDefinition<
  typeof args
> = {
  name: "employees_update-onboarding-documents-config",
  description: `Update an employee's onboarding documents config

Indicate whether to include the Form I-9 for an employee during the onboarding process.

scope: \`employees:manage\``,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await employeesUpdateOnboardingDocumentsConfig(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};

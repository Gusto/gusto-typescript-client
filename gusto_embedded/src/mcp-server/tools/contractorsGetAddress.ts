/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { contractorsGetAddress } from "../../funcs/contractorsGetAddress.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request:
    operations.GetV1ContractorsContractorUuidAddressRequest$inboundSchema,
};

export const tool$contractorsGetAddress: ToolDefinition<typeof args> = {
  name: "contractors_get-address",
  description: `Get a contractor address

The address of a contractor is used to determine certain tax information about them. Addresses are geocoded on create and update to ensure validity.

scope: \`contractors:read\``,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await contractorsGetAddress(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};

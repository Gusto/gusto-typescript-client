/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { contractorDocumentsGet } from "../../funcs/contractorDocumentsGet.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetV1ContractorDocumentRequest$inboundSchema,
};

export const tool$contractorDocumentsGet: ToolDefinition<typeof args> = {
  name: "contractor-documents_get",
  description: `Get a contractor document

Get a contractor document.

scope: \`contractor_documents:read\``,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await contractorDocumentsGet(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};

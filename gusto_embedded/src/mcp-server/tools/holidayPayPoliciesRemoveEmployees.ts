/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { holidayPayPoliciesRemoveEmployees } from "../../funcs/holidayPayPoliciesRemoveEmployees.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request:
    operations
      .PutCompaniesCompanyUuidHolidayPayPolicyRemoveRequest$inboundSchema,
};

export const tool$holidayPayPoliciesRemoveEmployees: ToolDefinition<
  typeof args
> = {
  name: "holiday-pay-policies_remove-employees",
  description: `Remove employees from a company's holiday pay policy

Remove employees from a company's holiday pay policy

scope: \`holiday_pay_policies:write\``,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await holidayPayPoliciesRemoveEmployees(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};

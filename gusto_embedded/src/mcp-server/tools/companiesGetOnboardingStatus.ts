/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { companiesGetOnboardingStatus } from "../../funcs/companiesGetOnboardingStatus.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetV1CompanyOnboardingStatusRequest$inboundSchema,
};

export const tool$companiesGetOnboardingStatus: ToolDefinition<typeof args> = {
  name: "companies_get-onboarding-status",
  description: `Get the company's onboarding status

Get company's onboarding status.
The data returned helps inform the required onboarding steps and respective completion status.

scope: \`company_onboarding_status:read\``,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await companiesGetOnboardingStatus(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};

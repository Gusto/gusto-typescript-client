/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { externalPayrollsCalculateTaxes } from "../../funcs/externalPayrollsCalculateTaxes.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetV1ExternalPayrollCalculateTaxesRequest$inboundSchema,
};

export const tool$externalPayrollsCalculateTaxes: ToolDefinition<typeof args> =
  {
    name: "external-payrolls_calculate-taxes",
    description: `Get tax suggestions for an external payroll

Get tax suggestions for an external payroll. Earnings and/or benefits
data must be saved prior to the calculation in order to retrieve accurate
tax calculation.

scope: \`external_payrolls:read\``,
    args,
    tool: async (client, args, ctx) => {
      const [result, apiCall] = await externalPayrollsCalculateTaxes(
        client,
        args.request,
        { fetchOptions: { signal: ctx.signal } },
      ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      const value = result.value;

      return formatResult(value, apiCall);
    },
  };

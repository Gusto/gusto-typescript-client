/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { externalPayrollsDelete } from "../../funcs/externalPayrollsDelete.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.DeleteV1ExternalPayrollRequest$inboundSchema,
};

export const tool$externalPayrollsDelete: ToolDefinition<typeof args> = {
  name: "external-payrolls_delete",
  description: `Delete an external payroll

Delete an external payroll.

scope: \`external_payrolls:write\``,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await externalPayrollsDelete(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(void 0, apiCall);
  },
};

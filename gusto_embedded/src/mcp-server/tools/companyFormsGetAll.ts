/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { companyFormsGetAll } from "../../funcs/companyFormsGetAll.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetV1CompanyFormsRequest$inboundSchema,
};

export const tool$companyFormsGetAll: ToolDefinition<typeof args> = {
  name: "company-forms_get-all",
  description: `Get all company forms

Get a list of all company's forms

scope: \`company_forms:read\``,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await companyFormsGetAll(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};

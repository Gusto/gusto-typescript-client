/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { externalPayrollsRetrieve } from "../../funcs/externalPayrollsRetrieve.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetV1ExternalPayrollRequest$inboundSchema,
};

export const tool$externalPayrollsRetrieve: ToolDefinition<typeof args> = {
  name: "external-payrolls_retrieve",
  description: `Get an external payroll

Get an external payroll for a given company.

scope: \`external_payrolls:read\``,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await externalPayrollsRetrieve(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};

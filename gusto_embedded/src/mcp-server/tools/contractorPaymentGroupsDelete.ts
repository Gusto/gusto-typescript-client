/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { contractorPaymentGroupsDelete } from "../../funcs/contractorPaymentGroupsDelete.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request:
    operations
      .DeleteV1ContractorPaymentGroupsContractorPaymentGroupIdRequest$inboundSchema,
};

export const tool$contractorPaymentGroupsDelete: ToolDefinition<typeof args> = {
  name: "contractor-payment-groups_delete",
  description: `Cancel a contractor payment group

Cancels a contractor payment group and all associated contractor payments. All contractor payments must be cancellable, unfunded.

scope: \`payrolls:run\``,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await contractorPaymentGroupsDelete(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(void 0, apiCall);
  },
};

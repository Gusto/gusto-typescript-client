/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { departmentsRemovePeople } from "../../funcs/departmentsRemovePeople.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.PutRemovePeopleFromDepartmentRequest$inboundSchema,
};

export const tool$departmentsRemovePeople: ToolDefinition<typeof args> = {
  name: "departments_remove-people",
  description: `Remove people from a department

Remove employees and contractors from a department

scope: \`departments:write\`
`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await departmentsRemovePeople(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};

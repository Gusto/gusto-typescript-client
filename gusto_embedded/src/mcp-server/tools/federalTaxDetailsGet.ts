/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { federalTaxDetailsGet } from "../../funcs/federalTaxDetailsGet.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request:
    operations.GetV1CompaniesCompanyIdFederalTaxDetailsRequest$inboundSchema,
};

export const tool$federalTaxDetailsGet: ToolDefinition<typeof args> = {
  name: "federal-tax-details_get",
  description: `Get Federal Tax Details

Fetches attributes relevant for a company's federal taxes.

scope: \`company_federal_taxes:read\``,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await federalTaxDetailsGet(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};

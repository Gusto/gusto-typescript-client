/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { companyAttachmentsGetList } from "../../funcs/companyAttachmentsGetList.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetV1CompaniesAttachmentsRequest$inboundSchema,
};

export const tool$companyAttachmentsGetList: ToolDefinition<typeof args> = {
  name: "company-attachments_get-list",
  description: `Get List of Company Attachments

Retrieve a list of all the attachments uploaded by the company.

scope: \`company_attachments:read\``,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await companyAttachmentsGetList(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};

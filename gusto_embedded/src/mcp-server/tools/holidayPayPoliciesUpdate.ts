/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { holidayPayPoliciesUpdate } from "../../funcs/holidayPayPoliciesUpdate.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request:
    operations.PutCompaniesCompanyUuidHolidayPayPolicyRequest$inboundSchema,
};

export const tool$holidayPayPoliciesUpdate: ToolDefinition<typeof args> = {
  name: "holiday-pay-policies_update",
  description: `Update a company's holiday pay policy

Update a company's holiday pay policy

scope: \`holiday_pay_policies:write\``,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await holidayPayPoliciesUpdate(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};

/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { contractorsDelete } from "../../funcs/contractorsDelete.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.DeleteV1ContractorsContractorUuidRequest$inboundSchema,
};

export const tool$contractorsDelete: ToolDefinition<typeof args> = {
  name: "contractors_delete",
  description: `Delete a contractor

A contractor can only be deleted when there are no contractor payments.

scope: \`contractors:manage\``,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await contractorsDelete(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(void 0, apiCall);
  },
};

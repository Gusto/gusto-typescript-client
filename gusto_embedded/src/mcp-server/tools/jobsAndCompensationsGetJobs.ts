/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { jobsAndCompensationsGetJobs } from "../../funcs/jobsAndCompensationsGetJobs.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetV1EmployeesEmployeeIdJobsRequest$inboundSchema,
};

export const tool$jobsAndCompensationsGetJobs: ToolDefinition<typeof args> = {
  name: "jobs-and-compensations_get-jobs",
  description: `Get jobs for an employee

Get all of the jobs that an employee holds.

scope: \`jobs:read\``,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await jobsAndCompensationsGetJobs(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};

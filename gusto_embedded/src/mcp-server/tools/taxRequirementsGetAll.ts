/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { taxRequirementsGetAll } from "../../funcs/taxRequirementsGetAll.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request:
    operations.GetV1CompaniesCompanyUuidTaxRequirementsRequest$inboundSchema,
};

export const tool$taxRequirementsGetAll: ToolDefinition<typeof args> = {
  name: "tax-requirements_get-all",
  description: `Get All Tax Requirement States

Returns objects describing the states that have tax requirements for the company

scope: \`company_tax_requirements:read\``,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await taxRequirementsGetAll(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};

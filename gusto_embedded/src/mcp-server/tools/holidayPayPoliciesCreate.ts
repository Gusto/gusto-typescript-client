/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { holidayPayPoliciesCreate } from "../../funcs/holidayPayPoliciesCreate.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request:
    operations.PostCompaniesCompanyUuidHolidayPayPolicyRequest$inboundSchema,
};

export const tool$holidayPayPoliciesCreate: ToolDefinition<typeof args> = {
  name: "holiday-pay-policies_create",
  description: `Create a holiday pay policy for a company

Create a holiday pay policy for a company

scope: \`holiday_pay_policies:write\``,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await holidayPayPoliciesCreate(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};

/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { contractorPaymentMethodUpdate } from "../../funcs/contractorPaymentMethodUpdate.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request:
    operations.PutV1ContractorsContractorIdPaymentMethodRequest$inboundSchema,
};

export const tool$contractorPaymentMethodUpdate: ToolDefinition<typeof args> = {
  name: "contractor-payment-method_update",
  description: `Update a contractor's payment method

Updates a contractor's payment method. Note that creating a contractor
bank account will also update the contractor's payment method.

scope: \`contractor_payment_methods:write\``,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await contractorPaymentMethodUpdate(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};

/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { historicalEmployeesUpdate } from "../../funcs/historicalEmployeesUpdate.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.PutV1HistoricalEmployeesRequest$inboundSchema,
};

export const tool$historicalEmployeesUpdate: ToolDefinition<typeof args> = {
  name: "historical-employees_update",
  description: `Update a historical employee

Update a historical employee, an employee that was previously dismissed from the company in the current year.

scope: \`employees:manage\``,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await historicalEmployeesUpdate(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};

/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { locationsUpdate } from "../../funcs/locationsUpdate.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.PutV1LocationsLocationIdRequest$inboundSchema,
};

export const tool$locationsUpdate: ToolDefinition<typeof args> = {
  name: "locations_update",
  description: `Update a location

Update a location.

scope: \`companies.write\``,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await locationsUpdate(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};

/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { paymentConfigsUpdate } from "../../funcs/paymentConfigsUpdate.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.PutV1CompanyPaymentConfigsRequest$inboundSchema,
};

export const tool$paymentConfigsUpdate: ToolDefinition<typeof args> = {
  name: "payment-configs_update",
  description: `Update a company's payment configs

Update payment speed and fast payment limit for a company. At least one of \`payment_speed\` or \`fast_payment_limit\` parameters is required. 1-day option is only applicable to partners that opt in.

scope: \`company_payment_configs:write\``,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await paymentConfigsUpdate(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};

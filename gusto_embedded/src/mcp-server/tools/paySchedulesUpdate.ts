/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { paySchedulesUpdate } from "../../funcs/paySchedulesUpdate.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request:
    operations
      .PutV1CompaniesCompanyIdPaySchedulesPayScheduleIdRequest$inboundSchema,
};

export const tool$paySchedulesUpdate: ToolDefinition<typeof args> = {
  name: "pay-schedules_update",
  description: `Update a pay schedule

Updates a pay schedule.

scope: \`pay_schedules:write\``,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await paySchedulesUpdate(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};

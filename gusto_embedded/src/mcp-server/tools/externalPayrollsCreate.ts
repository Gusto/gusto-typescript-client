/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { externalPayrollsCreate } from "../../funcs/externalPayrollsCreate.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.PostV1ExternalPayrollRequest$inboundSchema,
};

export const tool$externalPayrollsCreate: ToolDefinition<typeof args> = {
  name: "external-payrolls_create",
  description: `Create a new external payroll for a company

Creates a new external payroll for the company.

scope: \`external_payrolls:write\``,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await externalPayrollsCreate(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};

/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { employeesCreateHistorical } from "../../funcs/employeesCreateHistorical.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.PostV1HistoricalEmployeesRequest$inboundSchema,
};

export const tool$employeesCreateHistorical: ToolDefinition<typeof args> = {
  name: "employees_create-historical",
  description: `Create a historical employee

Create a historical employee, an employee that was previously dismissed from the company in the current year.

scope: \`employees:manage\``,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await employeesCreateHistorical(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};

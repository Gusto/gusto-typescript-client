/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { externalPayrollsListTaxLiabilities } from "../../funcs/externalPayrollsListTaxLiabilities.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetV1TaxLiabilitiesRequest$inboundSchema,
};

export const tool$externalPayrollsListTaxLiabilities: ToolDefinition<
  typeof args
> = {
  name: "external-payrolls_list-tax-liabilities",
  description: `Get tax liabilities

Get tax liabilities from aggregate external payrolls for a company.

scope: \`external_payrolls:read\``,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await externalPayrollsListTaxLiabilities(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};

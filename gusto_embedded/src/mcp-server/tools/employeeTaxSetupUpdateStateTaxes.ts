/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { employeeTaxSetupUpdateStateTaxes } from "../../funcs/employeeTaxSetupUpdateStateTaxes.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.PutV1EmployeesEmployeeIdStateTaxesRequest$inboundSchema,
};

export const tool$employeeTaxSetupUpdateStateTaxes: ToolDefinition<
  typeof args
> = {
  name: "employee-tax-setup_update-state-taxes",
  description: `Update an employee's state taxes

Update attributes relevant for an employee's state taxes.

As described for the GET endpoint, the answers must be supplied in the effective-dated format, but currently only a single answer will be accepted - \`valid_from\` and \`valid_up_to\` must be \`"2010-01-01"\` and \`null\` respectively.

scope: \`employee_state_taxes:write\``,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await employeeTaxSetupUpdateStateTaxes(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};

/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { reportsGetTemplate } from "../../funcs/reportsGetTemplate.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request:
    operations
      .GetCompaniesCompanyUuidReportTemplatesReportTypeRequest$inboundSchema,
};

export const tool$reportsGetTemplate: ToolDefinition<typeof args> = {
  name: "reports_get-template",
  description: `Get a report template

Get a company's report template. The only supported report type is \`payroll_journal\`. The resulting columns and groupings from this endpoint can be used as a guidance to create the report using the POST create report endpoint.

scope: \`company_reports:write\``,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await reportsGetTemplate(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};

/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { contractorPaymentGroupsGetList } from "../../funcs/contractorPaymentGroupsGetList.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request:
    operations
      .GetV1CompaniesCompanyIdContractorPaymentGroupsRequest$inboundSchema,
};

export const tool$contractorPaymentGroupsGetList: ToolDefinition<typeof args> =
  {
    name: "contractor-payment-groups_get-list",
    description: `Get contractor payment groups for a company

Returns a list of minimal contractor payment groups within a given time period, including totals but not associated contractor payments.

scope: \`payrolls:read\``,
    args,
    tool: async (client, args, ctx) => {
      const [result, apiCall] = await contractorPaymentGroupsGetList(
        client,
        args.request,
        { fetchOptions: { signal: ctx.signal } },
      ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      const value = result.value;

      return formatResult(value, apiCall);
    },
  };

/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { employeeFormsGenerateW2 } from "../../funcs/employeeFormsGenerateW2.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.PostV1SandboxGenerateW2Request$inboundSchema,
};

export const tool$employeeFormsGenerateW2: ToolDefinition<typeof args> = {
  name: "employee-forms_generate-w2",
  description: `Generate a W2 form [DEMO]

> 🚧 Demo action
>
> This action is only available in the Demo environment

Generates a W2 document for testing purposes.

scope: \`employees:write\``,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await employeeFormsGenerateW2(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};

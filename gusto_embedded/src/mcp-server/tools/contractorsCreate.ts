/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { contractorsCreate } from "../../funcs/contractorsCreate.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request:
    operations.PostV1CompaniesCompanyUuidContractorsRequest$inboundSchema,
};

export const tool$contractorsCreate: ToolDefinition<typeof args> = {
  name: "contractors_create",
  description: `Create a contractor

Create an individual or business contractor.

scope: \`contractors:manage\``,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await contractorsCreate(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};

/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { employeeBenefitsRetrieve } from "../../funcs/employeeBenefitsRetrieve.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request:
    operations.GetV1EmployeeBenefitsEmployeeBenefitIdRequest$inboundSchema,
};

export const tool$employeeBenefitsRetrieve: ToolDefinition<typeof args> = {
  name: "employee-benefits_retrieve",
  description: `Get an employee benefit

Employee benefits represent an employee enrolled in a particular company benefit. It includes information specific to that employee’s enrollment.

Benefits containing PHI are only visible to applications with the \`employee_benefits:read:phi\` scope.

scope: \`employee_benefits:read\``,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await employeeBenefitsRetrieve(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};

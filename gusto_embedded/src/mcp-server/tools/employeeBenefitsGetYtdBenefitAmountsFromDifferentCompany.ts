/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { employeeBenefitsGetYtdBenefitAmountsFromDifferentCompany } from "../../funcs/employeeBenefitsGetYtdBenefitAmountsFromDifferentCompany.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request:
    operations
      .GetEmployeeYtdBenefitAmountsFromDifferentCompanyRequest$inboundSchema,
};

export const tool$employeeBenefitsGetYtdBenefitAmountsFromDifferentCompany:
  ToolDefinition<typeof args> = {
    name: "employee-benefits_get-ytd-benefit-amounts-from-different-company",
    description: `Get year-to-date benefit amounts from a different company

Retrieves year-to-date benefit amounts that were contributed at a different company for the specified employee.
Returns benefit amounts for the requested tax year (defaults to current year if not specified).

This endpoint only supports retrieving outside contributions for 401(k) benefits.

scope: \`employee_benefits:read\``,
    args,
    tool: async (client, args, ctx) => {
      const [result, apiCall] =
        await employeeBenefitsGetYtdBenefitAmountsFromDifferentCompany(
          client,
          args.request,
          { fetchOptions: { signal: ctx.signal } },
        ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      const value = result.value;

      return formatResult(value, apiCall);
    },
  };

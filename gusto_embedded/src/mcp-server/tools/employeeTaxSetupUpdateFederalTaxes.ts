/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { employeeTaxSetupUpdateFederalTaxes } from "../../funcs/employeeTaxSetupUpdateFederalTaxes.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.PutV1EmployeesEmployeeIdFederalTaxesRequest$inboundSchema,
};

export const tool$employeeTaxSetupUpdateFederalTaxes: ToolDefinition<
  typeof args
> = {
  name: "employee-tax-setup_update-federal-taxes",
  description: `Update an employee's federal taxes

Update attributes relevant for an employee's federal taxes.

scope: \`employee_federal_taxes:write\``,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await employeeTaxSetupUpdateFederalTaxes(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};

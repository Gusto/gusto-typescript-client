/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { employeesUpdate } from "../../funcs/employeesUpdate.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.PutV1EmployeesRequest$inboundSchema,
};

export const tool$employeesUpdate: ToolDefinition<typeof args> = {
  name: "employees_update",
  description: `Update an employee

Update an employee.

scope: \`employees:write\``,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await employeesUpdate(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};

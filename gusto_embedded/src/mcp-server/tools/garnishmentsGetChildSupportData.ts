/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { garnishmentsGetChildSupportData } from "../../funcs/garnishmentsGetChildSupportData.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetV1GarnishmentsChildSupportRequest$inboundSchema,
};

export const tool$garnishmentsGetChildSupportData: ToolDefinition<typeof args> =
  {
    name: "garnishments_get-child-support-data",
    description: `Get child support garnishment data

Agency data and requirements to be used for creating child support garnishments

scope: \`garnishments:read\``,
    args,
    tool: async (client, args, ctx) => {
      const [result, apiCall] = await garnishmentsGetChildSupportData(
        client,
        args.request,
        { fetchOptions: { signal: ctx.signal } },
      ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      const value = result.value;

      return formatResult(value, apiCall);
    },
  };

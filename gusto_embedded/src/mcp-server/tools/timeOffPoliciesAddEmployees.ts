/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { timeOffPoliciesAddEmployees } from "../../funcs/timeOffPoliciesAddEmployees.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request:
    operations
      .PutVersionTimeOffPoliciesTimeOffPolicyUuidAddEmployeesRequest$inboundSchema,
};

export const tool$timeOffPoliciesAddEmployees: ToolDefinition<typeof args> = {
  name: "time-off-policies_add-employees",
  description: `Add employees to a time off policy

Add employees to a time off policy. Employees are required to have at least one job to be added to a time off policy. Accepts starting balances for non-unlimited policies

scope: \`time_off_policies:write\``,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await timeOffPoliciesAddEmployees(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};

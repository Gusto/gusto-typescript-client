/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { employeeFormsGet } from "../../funcs/employeeFormsGet.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetV1EmployeeFormRequest$inboundSchema,
};

export const tool$employeeFormsGet: ToolDefinition<typeof args> = {
  name: "employee-forms_get",
  description: `Get an employee form

Get an employee form

scope: \`employee_forms:read\``,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await employeeFormsGet(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};

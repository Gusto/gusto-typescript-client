/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { paymentConfigsGet } from "../../funcs/paymentConfigsGet.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetV1CompanyPaymentConfigsRequest$inboundSchema,
};

export const tool$paymentConfigsGet: ToolDefinition<typeof args> = {
  name: "payment-configs_get",
  description: `Get a company's payment configs

Get payment speed for the company and fast payment limit (1-day is only applicable to partners that opt in).

scope: \`company_payment_configs:read\``,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await paymentConfigsGet(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};

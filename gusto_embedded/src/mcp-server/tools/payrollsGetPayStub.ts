/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { payrollsGetPayStub } from "../../funcs/payrollsGetPayStub.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request:
    operations
      .GetV1PayrollsPayrollUuidEmployeesEmployeeUuidPayStubRequest$inboundSchema,
};

export const tool$payrollsGetPayStub: ToolDefinition<typeof args> = {
  name: "payrolls_get-pay-stub",
  description: `Get an employee pay stub (pdf)

Get an employee's pay stub for the specified payroll. By default, an application/pdf response will be returned. No other content types are currently supported, but may be supported in the future.

scope: \`pay_stubs:read\``,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await payrollsGetPayStub(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(void 0, apiCall);
  },
};

/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { paySchedulesGetAll } from "../../funcs/paySchedulesGetAll.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetV1CompaniesCompanyIdPaySchedulesRequest$inboundSchema,
};

export const tool$paySchedulesGetAll: ToolDefinition<typeof args> = {
  name: "pay-schedules_get-all",
  description: `Get the pay schedules for a company

The pay schedule object in Gusto captures the details of when employees work and when they should be paid. A company can have multiple pay schedules.

scope: \`pay_schedules:read\``,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await paySchedulesGetAll(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};

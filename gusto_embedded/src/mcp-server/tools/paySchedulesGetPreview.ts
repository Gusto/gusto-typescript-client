/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { paySchedulesGetPreview } from "../../funcs/paySchedulesGetPreview.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request:
    operations.GetV1CompaniesCompanyIdPaySchedulesPreviewRequest$inboundSchema,
};

export const tool$paySchedulesGetPreview: ToolDefinition<typeof args> = {
  name: "pay-schedules_get-preview",
  description: `Preview pay schedule dates

Provides a preview of a pay schedule with the specified parameters for the next 18 months.

scope: \`pay_schedules:write\``,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await paySchedulesGetPreview(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};

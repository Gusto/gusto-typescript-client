/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { companyBenefitsGetRequirements } from "../../funcs/companyBenefitsGetRequirements.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetV1BenefitsBenefitsIdRequirementsRequest$inboundSchema,
};

export const tool$companyBenefitsGetRequirements: ToolDefinition<typeof args> =
  {
    name: "company-benefits_get-requirements",
    description: `Get benefit fields requirements by ID

Returns field requirements for the requested benefit type.

scope: \`benefits:read\``,
    args,
    tool: async (client, args, ctx) => {
      const [result, apiCall] = await companyBenefitsGetRequirements(
        client,
        args.request,
        { fetchOptions: { signal: ctx.signal } },
      ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      const value = result.value;

      return formatResult(value, apiCall);
    },
  };

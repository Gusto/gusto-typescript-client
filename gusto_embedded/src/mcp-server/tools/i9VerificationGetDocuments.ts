/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { i9VerificationGetDocuments } from "../../funcs/i9VerificationGetDocuments.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request:
    operations
      .GetV1EmployeesEmployeeIdI9AuthorizationDocumentsRequest$inboundSchema,
};

export const tool$i9VerificationGetDocuments: ToolDefinition<typeof args> = {
  name: "i9-verification_get-documents",
  description: `Get an employee's I-9 verification documents

An employee's I-9 verification documents are the documents an employee has provided the employer to verify their identity and authorization to work in the United States.

scope: \`i9_authorizations:read\``,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await i9VerificationGetDocuments(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};

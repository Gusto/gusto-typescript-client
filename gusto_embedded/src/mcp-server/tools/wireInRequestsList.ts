/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { wireInRequestsList } from "../../funcs/wireInRequestsList.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request:
    operations.GetCompaniesCompanyUuidWireInRequestUuidRequest$inboundSchema,
};

export const tool$wireInRequestsList: ToolDefinition<typeof args> = {
  name: "wire-in-requests_list",
  description: `Get all Wire In Requests for a company

Fetches all Wire In Requests for a company.

scope: \`payrolls:read\``,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await wireInRequestsList(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};

/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { employeeEmploymentsRehire } from "../../funcs/employeeEmploymentsRehire.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.PutV1EmployeesEmployeeIdRehireRequest$inboundSchema,
};

export const tool$employeeEmploymentsRehire: ToolDefinition<typeof args> = {
  name: "employee-employments_rehire",
  description: `Update an employee rehire

Update an employee's rehire.

scope: \`employments:write\``,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await employeeEmploymentsRehire(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};

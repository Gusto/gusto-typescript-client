/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { i9VerificationDeleteDocument } from "../../funcs/i9VerificationDeleteDocument.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request:
    operations
      .DeleteV1EmployeesEmployeeIdI9AuthorizationDocumentsDocumentIdRequest$inboundSchema,
};

export const tool$i9VerificationDeleteDocument: ToolDefinition<typeof args> = {
  name: "i9-verification_delete-document",
  description: `Delete an employee's I-9 verification document

An employee's I-9 verification documents are the documents an employee has provided the employer to verify their identity and authorization to work in the United States. This endpoint deletes a specific verification document.

scope: \`i9_authorizations:manage\``,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await i9VerificationDeleteDocument(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(void 0, apiCall);
  },
};

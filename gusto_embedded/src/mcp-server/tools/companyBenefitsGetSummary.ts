/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { companyBenefitsGetSummary } from "../../funcs/companyBenefitsGetSummary.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetV1BenefitsCompanyBenefitIdSummaryRequest$inboundSchema,
};

export const tool$companyBenefitsGetSummary: ToolDefinition<typeof args> = {
  name: "company-benefits_get-summary",
  description: `Get company benefit summary by company benefit id.

Returns summary benefit data for the requested company benefit id.

Benefits containing PHI are only visible to applications with the \`company_benefits:read:phi\` scope.

scope: \`company_benefits:read\``,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await companyBenefitsGetSummary(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};

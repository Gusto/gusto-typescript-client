/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { employeeAddressesUpdateWorkAddress } from "../../funcs/employeeAddressesUpdateWorkAddress.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.PutV1WorkAddressesWorkAddressUuidRequest$inboundSchema,
};

export const tool$employeeAddressesUpdateWorkAddress: ToolDefinition<
  typeof args
> = {
  name: "employee-addresses_update-work-address",
  description: `Update an employee work address

The work address of an employee is used for payroll tax purposes.

scope: \`employees:manage\``,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await employeeAddressesUpdateWorkAddress(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};

/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { contractorFormsGenerate1099 } from "../../funcs/contractorFormsGenerate1099.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.PostV1SandboxGenerate1099Request$inboundSchema,
};

export const tool$contractorFormsGenerate1099: ToolDefinition<typeof args> = {
  name: "contractor-forms_generate1099",
  description: `Generate a 1099 form [DEMO]

> 🚧 Demo action
>
> This action is only available in the Demo environment

Generates a 1099 document for testing purposes.

scope: \`contractors:write\``,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await contractorFormsGenerate1099(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};

/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { payrollsGetApprovedReversals } from "../../funcs/payrollsGetApprovedReversals.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request:
    operations.GetV1CompaniesCompanyIdPayrollReversalsRequest$inboundSchema,
};

export const tool$payrollsGetApprovedReversals: ToolDefinition<typeof args> = {
  name: "payrolls_get-approved-reversals",
  description: `Get approved payroll reversals

Returns all approved Payroll Reversals for a Company.

scope: \`payrolls:read\``,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await payrollsGetApprovedReversals(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};

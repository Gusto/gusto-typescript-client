/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { contractorDocumentsGetAll } from "../../funcs/contractorDocumentsGetAll.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetV1ContractorDocumentsRequest$inboundSchema,
};

export const tool$contractorDocumentsGetAll: ToolDefinition<typeof args> = {
  name: "contractor-documents_get-all",
  description: `Get all contractor documents

Get a list of all contractor's documents

scope: \`contractor_documents:read\``,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await contractorDocumentsGetAll(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};

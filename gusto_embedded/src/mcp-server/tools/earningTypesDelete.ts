/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { earningTypesDelete } from "../../funcs/earningTypesDelete.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request:
    operations
      .DeleteV1CompaniesCompanyIdEarningTypesEarningTypeUuidRequest$inboundSchema,
};

export const tool$earningTypesDelete: ToolDefinition<typeof args> = {
  name: "earning-types_delete",
  description: `Deactivate an earning type

Deactivate an earning type.

scope: \`payrolls:write\``,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await earningTypesDelete(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(void 0, apiCall);
  },
};

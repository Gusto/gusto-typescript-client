/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { introspectionRefreshToken } from "../../funcs/introspectionRefreshToken.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.RefreshAccessTokenRequest$inboundSchema,
};

export const tool$introspectionRefreshToken: ToolDefinition<typeof args> = {
  name: "introspection_refresh-token",
  description: `Refresh access token

Exchange a refresh token for a new access token.

The previous \`refresh_token\` will be revoked on the first usage of the new \`access_token\`.

The \`expires_in\` value is provided in seconds from when the \`access_token\` was generated.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await introspectionRefreshToken(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};

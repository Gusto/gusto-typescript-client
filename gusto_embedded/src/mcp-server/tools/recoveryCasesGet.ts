/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { recoveryCasesGet } from "../../funcs/recoveryCasesGet.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetRecoveryCasesRequest$inboundSchema,
};

export const tool$recoveryCasesGet: ToolDefinition<typeof args> = {
  name: "recovery-cases_get",
  description: `Get all recovery cases for a company

Fetch all recovery cases for a company.

scope: \`recovery_cases:read\``,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await recoveryCasesGet(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};

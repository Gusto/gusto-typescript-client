/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { departmentsAddPeople } from "../../funcs/departmentsAddPeople.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.PutAddPeopleToDepartmentRequest$inboundSchema,
};

export const tool$departmentsAddPeople: ToolDefinition<typeof args> = {
  name: "departments_add-people",
  description: `Add people to a department

Add employees and contractors to a department

scope: \`departments:write\`
`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await departmentsAddPeople(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};

/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import {
  InvalidateQueryFilters,
  QueryClient,
  QueryFunctionContext,
  QueryKey,
  useQuery,
  UseQueryResult,
  useSuspenseQuery,
  UseSuspenseQueryResult,
} from "@tanstack/react-query";
import { GustoEmbeddedCore } from "../core.js";
import { paySchedulesList } from "../funcs/paySchedulesList.js";
import { combineSignals } from "../lib/primitives.js";
import { RequestOptions } from "../lib/sdks.js";
import * as components from "../models/components/index.js";
import * as operations from "../models/operations/index.js";
import { unwrapAsync } from "../types/fp.js";
import { useGustoEmbeddedContext } from "./_context.js";
import {
  QueryHookOptions,
  SuspenseQueryHookOptions,
  TupleToPrefixes,
} from "./_types.js";

export type PaySchedulesListQueryData = Array<components.PayPeriod>;

/**
 * Get pay periods for a company
 *
 * @remarks
 * Pay periods are the foundation of payroll. Compensation, time & attendance, taxes, and expense reports all rely on when they happened. To begin submitting information for a given payroll, we need to agree on the time period.
 *
 * By default, this endpoint returns pay periods starting from 6 months ago to the date today.  Use the `start_date` and `end_date` parameters to change the scope of the response.  End dates can be up to 3 months in the future and there is no limit on start dates.
 *
 * Starting in version '2023-04-01', the eligible_employees attribute was removed from the response.  The eligible employees for a payroll are determined by the employee_compensations returned from the payrolls#prepare endpoint.
 *
 * scope: `payrolls:read`
 */
export function usePaySchedulesList(
  request: operations.GetV1CompaniesCompanyIdPayPeriodsRequest,
  options?: QueryHookOptions<PaySchedulesListQueryData>,
): UseQueryResult<PaySchedulesListQueryData, Error> {
  const client = useGustoEmbeddedContext();
  return useQuery({
    ...buildPaySchedulesListQuery(
      client,
      request,
      options,
    ),
    ...options,
  });
}

/**
 * Get pay periods for a company
 *
 * @remarks
 * Pay periods are the foundation of payroll. Compensation, time & attendance, taxes, and expense reports all rely on when they happened. To begin submitting information for a given payroll, we need to agree on the time period.
 *
 * By default, this endpoint returns pay periods starting from 6 months ago to the date today.  Use the `start_date` and `end_date` parameters to change the scope of the response.  End dates can be up to 3 months in the future and there is no limit on start dates.
 *
 * Starting in version '2023-04-01', the eligible_employees attribute was removed from the response.  The eligible employees for a payroll are determined by the employee_compensations returned from the payrolls#prepare endpoint.
 *
 * scope: `payrolls:read`
 */
export function usePaySchedulesListSuspense(
  request: operations.GetV1CompaniesCompanyIdPayPeriodsRequest,
  options?: SuspenseQueryHookOptions<PaySchedulesListQueryData>,
): UseSuspenseQueryResult<PaySchedulesListQueryData, Error> {
  const client = useGustoEmbeddedContext();
  return useSuspenseQuery({
    ...buildPaySchedulesListQuery(
      client,
      request,
      options,
    ),
    ...options,
  });
}

export function prefetchPaySchedulesList(
  queryClient: QueryClient,
  client$: GustoEmbeddedCore,
  request: operations.GetV1CompaniesCompanyIdPayPeriodsRequest,
): Promise<void> {
  return queryClient.prefetchQuery({
    ...buildPaySchedulesListQuery(
      client$,
      request,
    ),
  });
}

export function setPaySchedulesListData(
  client: QueryClient,
  queryKeyBase: [
    companyId: string,
    parameters: {
      startDate?: string | undefined;
      endDate?: string | undefined;
      payrollTypes?: string | undefined;
      xGustoAPIVersion?: components.VersionHeader | undefined;
    },
  ],
  data: PaySchedulesListQueryData,
): PaySchedulesListQueryData | undefined {
  const key = queryKeyPaySchedulesList(...queryKeyBase);

  return client.setQueryData<PaySchedulesListQueryData>(key, data);
}

export function invalidatePaySchedulesList(
  client: QueryClient,
  queryKeyBase: TupleToPrefixes<
    [
      companyId: string,
      parameters: {
        startDate?: string | undefined;
        endDate?: string | undefined;
        payrollTypes?: string | undefined;
        xGustoAPIVersion?: components.VersionHeader | undefined;
      },
    ]
  >,
  filters?: Omit<InvalidateQueryFilters, "queryKey" | "predicate" | "exact">,
): Promise<void> {
  return client.invalidateQueries({
    ...filters,
    queryKey: ["@gusto/embedded-api", "paySchedules", "list", ...queryKeyBase],
  });
}

export function invalidateAllPaySchedulesList(
  client: QueryClient,
  filters?: Omit<InvalidateQueryFilters, "queryKey" | "predicate" | "exact">,
): Promise<void> {
  return client.invalidateQueries({
    ...filters,
    queryKey: ["@gusto/embedded-api", "paySchedules", "list"],
  });
}

export function buildPaySchedulesListQuery(
  client$: GustoEmbeddedCore,
  request: operations.GetV1CompaniesCompanyIdPayPeriodsRequest,
  options?: RequestOptions,
): {
  queryKey: QueryKey;
  queryFn: (
    context: QueryFunctionContext,
  ) => Promise<PaySchedulesListQueryData>;
} {
  return {
    queryKey: queryKeyPaySchedulesList(request.companyId, {
      startDate: request.startDate,
      endDate: request.endDate,
      payrollTypes: request.payrollTypes,
      xGustoAPIVersion: request.xGustoAPIVersion,
    }),
    queryFn: async function paySchedulesListQueryFn(
      ctx,
    ): Promise<PaySchedulesListQueryData> {
      const sig = combineSignals(ctx.signal, options?.fetchOptions?.signal);
      const mergedOptions = {
        ...options,
        fetchOptions: { ...options?.fetchOptions, signal: sig },
      };

      return unwrapAsync(paySchedulesList(
        client$,
        request,
        mergedOptions,
      ));
    },
  };
}

export function queryKeyPaySchedulesList(
  companyId: string,
  parameters: {
    startDate?: string | undefined;
    endDate?: string | undefined;
    payrollTypes?: string | undefined;
    xGustoAPIVersion?: components.VersionHeader | undefined;
  },
): QueryKey {
  return ["@gusto/embedded-api", "paySchedules", "list", companyId, parameters];
}

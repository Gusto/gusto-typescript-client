/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import {
  InvalidateQueryFilters,
  QueryClient,
  QueryFunctionContext,
  QueryKey,
  useQuery,
  UseQueryResult,
  useSuspenseQuery,
  UseSuspenseQueryResult,
} from "@tanstack/react-query";
import { GustoEmbeddedCore } from "../core.js";
import { companiesBenefitsList } from "../funcs/companiesBenefitsList.js";
import { combineSignals } from "../lib/primitives.js";
import { RequestOptions } from "../lib/sdks.js";
import * as components from "../models/components/index.js";
import * as operations from "../models/operations/index.js";
import { unwrapAsync } from "../types/fp.js";
import { useGustoEmbeddedContext } from "./_context.js";
import {
  QueryHookOptions,
  SuspenseQueryHookOptions,
  TupleToPrefixes,
} from "./_types.js";

export type CompaniesBenefitsListQueryData = Array<components.CompanyBenefit>;

/**
 * Get benefits for a company
 *
 * @remarks
 * Company benefits represent the benefits that a company is offering to employees. This ties together a particular supported benefit with the company-specific information for the offering of that benefit.
 *
 * Note that company benefits can be deactivated only when no employees are enrolled.
 *
 * Benefits containing PHI are only visible to applications with the `company_benefits:read:phi` scope.
 *
 * scope: `company_benefits:read`
 */
export function useCompaniesBenefitsList(
  request: operations.GetV1CompaniesCompanyIdCompanyBenefitsRequest,
  options?: QueryHookOptions<CompaniesBenefitsListQueryData>,
): UseQueryResult<CompaniesBenefitsListQueryData, Error> {
  const client = useGustoEmbeddedContext();
  return useQuery({
    ...buildCompaniesBenefitsListQuery(
      client,
      request,
      options,
    ),
    ...options,
  });
}

/**
 * Get benefits for a company
 *
 * @remarks
 * Company benefits represent the benefits that a company is offering to employees. This ties together a particular supported benefit with the company-specific information for the offering of that benefit.
 *
 * Note that company benefits can be deactivated only when no employees are enrolled.
 *
 * Benefits containing PHI are only visible to applications with the `company_benefits:read:phi` scope.
 *
 * scope: `company_benefits:read`
 */
export function useCompaniesBenefitsListSuspense(
  request: operations.GetV1CompaniesCompanyIdCompanyBenefitsRequest,
  options?: SuspenseQueryHookOptions<CompaniesBenefitsListQueryData>,
): UseSuspenseQueryResult<CompaniesBenefitsListQueryData, Error> {
  const client = useGustoEmbeddedContext();
  return useSuspenseQuery({
    ...buildCompaniesBenefitsListQuery(
      client,
      request,
      options,
    ),
    ...options,
  });
}

export function prefetchCompaniesBenefitsList(
  queryClient: QueryClient,
  client$: GustoEmbeddedCore,
  request: operations.GetV1CompaniesCompanyIdCompanyBenefitsRequest,
): Promise<void> {
  return queryClient.prefetchQuery({
    ...buildCompaniesBenefitsListQuery(
      client$,
      request,
    ),
  });
}

export function setCompaniesBenefitsListData(
  client: QueryClient,
  queryKeyBase: [
    companyId: string,
    parameters: {
      enrollmentCount?: boolean | undefined;
      xGustoAPIVersion?: components.VersionHeader | undefined;
    },
  ],
  data: CompaniesBenefitsListQueryData,
): CompaniesBenefitsListQueryData | undefined {
  const key = queryKeyCompaniesBenefitsList(...queryKeyBase);

  return client.setQueryData<CompaniesBenefitsListQueryData>(key, data);
}

export function invalidateCompaniesBenefitsList(
  client: QueryClient,
  queryKeyBase: TupleToPrefixes<
    [
      companyId: string,
      parameters: {
        enrollmentCount?: boolean | undefined;
        xGustoAPIVersion?: components.VersionHeader | undefined;
      },
    ]
  >,
  filters?: Omit<InvalidateQueryFilters, "queryKey" | "predicate" | "exact">,
): Promise<void> {
  return client.invalidateQueries({
    ...filters,
    queryKey: ["@gusto/embedded-api", "benefits", "list", ...queryKeyBase],
  });
}

export function invalidateAllCompaniesBenefitsList(
  client: QueryClient,
  filters?: Omit<InvalidateQueryFilters, "queryKey" | "predicate" | "exact">,
): Promise<void> {
  return client.invalidateQueries({
    ...filters,
    queryKey: ["@gusto/embedded-api", "benefits", "list"],
  });
}

export function buildCompaniesBenefitsListQuery(
  client$: GustoEmbeddedCore,
  request: operations.GetV1CompaniesCompanyIdCompanyBenefitsRequest,
  options?: RequestOptions,
): {
  queryKey: QueryKey;
  queryFn: (
    context: QueryFunctionContext,
  ) => Promise<CompaniesBenefitsListQueryData>;
} {
  return {
    queryKey: queryKeyCompaniesBenefitsList(request.companyId, {
      enrollmentCount: request.enrollmentCount,
      xGustoAPIVersion: request.xGustoAPIVersion,
    }),
    queryFn: async function companiesBenefitsListQueryFn(
      ctx,
    ): Promise<CompaniesBenefitsListQueryData> {
      const sig = combineSignals(ctx.signal, options?.fetchOptions?.signal);
      const mergedOptions = {
        ...options,
        fetchOptions: { ...options?.fetchOptions, signal: sig },
      };

      return unwrapAsync(companiesBenefitsList(
        client$,
        request,
        mergedOptions,
      ));
    },
  };
}

export function queryKeyCompaniesBenefitsList(
  companyId: string,
  parameters: {
    enrollmentCount?: boolean | undefined;
    xGustoAPIVersion?: components.VersionHeader | undefined;
  },
): QueryKey {
  return ["@gusto/embedded-api", "benefits", "list", companyId, parameters];
}

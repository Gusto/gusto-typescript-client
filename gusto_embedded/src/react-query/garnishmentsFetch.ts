/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import {
  InvalidateQueryFilters,
  QueryClient,
  QueryFunctionContext,
  QueryKey,
  useQuery,
  UseQueryResult,
  useSuspenseQuery,
  UseSuspenseQueryResult,
} from "@tanstack/react-query";
import { GustoEmbeddedCore } from "../core.js";
import { garnishmentsFetch } from "../funcs/garnishmentsFetch.js";
import { combineSignals } from "../lib/primitives.js";
import { RequestOptions } from "../lib/sdks.js";
import * as components from "../models/components/index.js";
import * as operations from "../models/operations/index.js";
import { unwrapAsync } from "../types/fp.js";
import { useGustoEmbeddedContext } from "./_context.js";
import {
  QueryHookOptions,
  SuspenseQueryHookOptions,
  TupleToPrefixes,
} from "./_types.js";

export type GarnishmentsFetchQueryData = components.Garnishment;

/**
 * Get a garnishment
 *
 * @remarks
 * Garnishments, or employee deductions, are fixed amounts or percentages deducted from an employee’s pay. They can be deducted a specific number of times or on a recurring basis. Garnishments can also have maximum deductions on a yearly or per-pay-period bases. Common uses for garnishments are court-ordered payments for child support or back taxes. Some companies provide loans to their employees that are repaid via garnishments.
 *
 * scope: `garnishments:read`
 */
export function useGarnishmentsFetch(
  request: operations.GetV1GarnishmentsGarnishmentIdRequest,
  options?: QueryHookOptions<GarnishmentsFetchQueryData>,
): UseQueryResult<GarnishmentsFetchQueryData, Error> {
  const client = useGustoEmbeddedContext();
  return useQuery({
    ...buildGarnishmentsFetchQuery(
      client,
      request,
      options,
    ),
    ...options,
  });
}

/**
 * Get a garnishment
 *
 * @remarks
 * Garnishments, or employee deductions, are fixed amounts or percentages deducted from an employee’s pay. They can be deducted a specific number of times or on a recurring basis. Garnishments can also have maximum deductions on a yearly or per-pay-period bases. Common uses for garnishments are court-ordered payments for child support or back taxes. Some companies provide loans to their employees that are repaid via garnishments.
 *
 * scope: `garnishments:read`
 */
export function useGarnishmentsFetchSuspense(
  request: operations.GetV1GarnishmentsGarnishmentIdRequest,
  options?: SuspenseQueryHookOptions<GarnishmentsFetchQueryData>,
): UseSuspenseQueryResult<GarnishmentsFetchQueryData, Error> {
  const client = useGustoEmbeddedContext();
  return useSuspenseQuery({
    ...buildGarnishmentsFetchQuery(
      client,
      request,
      options,
    ),
    ...options,
  });
}

export function prefetchGarnishmentsFetch(
  queryClient: QueryClient,
  client$: GustoEmbeddedCore,
  request: operations.GetV1GarnishmentsGarnishmentIdRequest,
): Promise<void> {
  return queryClient.prefetchQuery({
    ...buildGarnishmentsFetchQuery(
      client$,
      request,
    ),
  });
}

export function setGarnishmentsFetchData(
  client: QueryClient,
  queryKeyBase: [
    garnishmentId: string,
    parameters: { xGustoAPIVersion?: components.VersionHeader | undefined },
  ],
  data: GarnishmentsFetchQueryData,
): GarnishmentsFetchQueryData | undefined {
  const key = queryKeyGarnishmentsFetch(...queryKeyBase);

  return client.setQueryData<GarnishmentsFetchQueryData>(key, data);
}

export function invalidateGarnishmentsFetch(
  client: QueryClient,
  queryKeyBase: TupleToPrefixes<
    [
      garnishmentId: string,
      parameters: { xGustoAPIVersion?: components.VersionHeader | undefined },
    ]
  >,
  filters?: Omit<InvalidateQueryFilters, "queryKey" | "predicate" | "exact">,
): Promise<void> {
  return client.invalidateQueries({
    ...filters,
    queryKey: ["@gusto/embedded-api", "Garnishments", "fetch", ...queryKeyBase],
  });
}

export function invalidateAllGarnishmentsFetch(
  client: QueryClient,
  filters?: Omit<InvalidateQueryFilters, "queryKey" | "predicate" | "exact">,
): Promise<void> {
  return client.invalidateQueries({
    ...filters,
    queryKey: ["@gusto/embedded-api", "Garnishments", "fetch"],
  });
}

export function buildGarnishmentsFetchQuery(
  client$: GustoEmbeddedCore,
  request: operations.GetV1GarnishmentsGarnishmentIdRequest,
  options?: RequestOptions,
): {
  queryKey: QueryKey;
  queryFn: (
    context: QueryFunctionContext,
  ) => Promise<GarnishmentsFetchQueryData>;
} {
  return {
    queryKey: queryKeyGarnishmentsFetch(request.garnishmentId, {
      xGustoAPIVersion: request.xGustoAPIVersion,
    }),
    queryFn: async function garnishmentsFetchQueryFn(
      ctx,
    ): Promise<GarnishmentsFetchQueryData> {
      const sig = combineSignals(ctx.signal, options?.fetchOptions?.signal);
      const mergedOptions = {
        ...options,
        fetchOptions: { ...options?.fetchOptions, signal: sig },
      };

      return unwrapAsync(garnishmentsFetch(
        client$,
        request,
        mergedOptions,
      ));
    },
  };
}

export function queryKeyGarnishmentsFetch(
  garnishmentId: string,
  parameters: { xGustoAPIVersion?: components.VersionHeader | undefined },
): QueryKey {
  return [
    "@gusto/embedded-api",
    "Garnishments",
    "fetch",
    garnishmentId,
    parameters,
  ];
}

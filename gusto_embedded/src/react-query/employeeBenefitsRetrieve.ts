/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import {
  InvalidateQueryFilters,
  QueryClient,
  QueryFunctionContext,
  QueryKey,
  useQuery,
  UseQueryResult,
  useSuspenseQuery,
  UseSuspenseQueryResult,
} from "@tanstack/react-query";
import { GustoEmbeddedCore } from "../core.js";
import { employeeBenefitsRetrieve } from "../funcs/employeeBenefitsRetrieve.js";
import { combineSignals } from "../lib/primitives.js";
import { RequestOptions } from "../lib/sdks.js";
import * as components from "../models/components/index.js";
import * as operations from "../models/operations/index.js";
import { unwrapAsync } from "../types/fp.js";
import { useGustoEmbeddedContext } from "./_context.js";
import {
  QueryHookOptions,
  SuspenseQueryHookOptions,
  TupleToPrefixes,
} from "./_types.js";

export type EmployeeBenefitsRetrieveQueryData =
  operations.GetV1EmployeeBenefitsEmployeeBenefitIdResponse;

/**
 * Get an employee benefit
 *
 * @remarks
 * Employee benefits represent an employee enrolled in a particular company benefit. It includes information specific to that employee’s enrollment.
 *
 * Benefits containing PHI are only visible to applications with the `employee_benefits:read:phi` scope.
 *
 * scope: `employee_benefits:read`
 */
export function useEmployeeBenefitsRetrieve(
  request: operations.GetV1EmployeeBenefitsEmployeeBenefitIdRequest,
  options?: QueryHookOptions<EmployeeBenefitsRetrieveQueryData>,
): UseQueryResult<EmployeeBenefitsRetrieveQueryData, Error> {
  const client = useGustoEmbeddedContext();
  return useQuery({
    ...buildEmployeeBenefitsRetrieveQuery(
      client,
      request,
      options,
    ),
    ...options,
  });
}

/**
 * Get an employee benefit
 *
 * @remarks
 * Employee benefits represent an employee enrolled in a particular company benefit. It includes information specific to that employee’s enrollment.
 *
 * Benefits containing PHI are only visible to applications with the `employee_benefits:read:phi` scope.
 *
 * scope: `employee_benefits:read`
 */
export function useEmployeeBenefitsRetrieveSuspense(
  request: operations.GetV1EmployeeBenefitsEmployeeBenefitIdRequest,
  options?: SuspenseQueryHookOptions<EmployeeBenefitsRetrieveQueryData>,
): UseSuspenseQueryResult<EmployeeBenefitsRetrieveQueryData, Error> {
  const client = useGustoEmbeddedContext();
  return useSuspenseQuery({
    ...buildEmployeeBenefitsRetrieveQuery(
      client,
      request,
      options,
    ),
    ...options,
  });
}

export function prefetchEmployeeBenefitsRetrieve(
  queryClient: QueryClient,
  client$: GustoEmbeddedCore,
  request: operations.GetV1EmployeeBenefitsEmployeeBenefitIdRequest,
): Promise<void> {
  return queryClient.prefetchQuery({
    ...buildEmployeeBenefitsRetrieveQuery(
      client$,
      request,
    ),
  });
}

export function setEmployeeBenefitsRetrieveData(
  client: QueryClient,
  queryKeyBase: [
    employeeBenefitId: string,
    parameters: { xGustoAPIVersion?: components.VersionHeader | undefined },
  ],
  data: EmployeeBenefitsRetrieveQueryData,
): EmployeeBenefitsRetrieveQueryData | undefined {
  const key = queryKeyEmployeeBenefitsRetrieve(...queryKeyBase);

  return client.setQueryData<EmployeeBenefitsRetrieveQueryData>(key, data);
}

export function invalidateEmployeeBenefitsRetrieve(
  client: QueryClient,
  queryKeyBase: TupleToPrefixes<
    [
      employeeBenefitId: string,
      parameters: { xGustoAPIVersion?: components.VersionHeader | undefined },
    ]
  >,
  filters?: Omit<InvalidateQueryFilters, "queryKey" | "predicate" | "exact">,
): Promise<void> {
  return client.invalidateQueries({
    ...filters,
    queryKey: [
      "@gusto/embedded-api",
      "employeeBenefits",
      "retrieve",
      ...queryKeyBase,
    ],
  });
}

export function invalidateAllEmployeeBenefitsRetrieve(
  client: QueryClient,
  filters?: Omit<InvalidateQueryFilters, "queryKey" | "predicate" | "exact">,
): Promise<void> {
  return client.invalidateQueries({
    ...filters,
    queryKey: ["@gusto/embedded-api", "employeeBenefits", "retrieve"],
  });
}

export function buildEmployeeBenefitsRetrieveQuery(
  client$: GustoEmbeddedCore,
  request: operations.GetV1EmployeeBenefitsEmployeeBenefitIdRequest,
  options?: RequestOptions,
): {
  queryKey: QueryKey;
  queryFn: (
    context: QueryFunctionContext,
  ) => Promise<EmployeeBenefitsRetrieveQueryData>;
} {
  return {
    queryKey: queryKeyEmployeeBenefitsRetrieve(request.employeeBenefitId, {
      xGustoAPIVersion: request.xGustoAPIVersion,
    }),
    queryFn: async function employeeBenefitsRetrieveQueryFn(
      ctx,
    ): Promise<EmployeeBenefitsRetrieveQueryData> {
      const sig = combineSignals(ctx.signal, options?.fetchOptions?.signal);
      const mergedOptions = {
        ...options,
        fetchOptions: { ...options?.fetchOptions, signal: sig },
      };

      return unwrapAsync(employeeBenefitsRetrieve(
        client$,
        request,
        mergedOptions,
      ));
    },
  };
}

export function queryKeyEmployeeBenefitsRetrieve(
  employeeBenefitId: string,
  parameters: { xGustoAPIVersion?: components.VersionHeader | undefined },
): QueryKey {
  return [
    "@gusto/embedded-api",
    "employeeBenefits",
    "retrieve",
    employeeBenefitId,
    parameters,
  ];
}

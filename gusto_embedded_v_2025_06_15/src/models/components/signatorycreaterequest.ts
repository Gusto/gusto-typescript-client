/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { remap as remap$ } from "../../lib/primitives.js";
import { RFCDate } from "../../types/rfcdate.js";

/**
 * The signatory's home address.
 */
export type HomeAddress = {
  street1: string;
  street2?: string | undefined;
  city: string;
  state: string;
  zip: string;
  country?: string | undefined;
};

/**
 * Request body for creating a signatory with complete information. All listed required fields must be provided.
 */
export type SignatoryCreateRequest = {
  /**
   * The signatory's first name.
   */
  firstName: string;
  /**
   * The signatory's last name.
   */
  lastName: string;
  /**
   * The signatory's title (e.g. CEO, President).
   */
  title: string;
  /**
   * The signatory's phone number.
   */
  phone: string;
  /**
   * The signatory's date of birth.
   */
  birthday: RFCDate;
  /**
   * The signatory's email address.
   */
  email: string;
  /**
   * The signatory's SSN.
   */
  ssn: string;
  /**
   * The signatory's home address.
   */
  homeAddress: HomeAddress;
};

/** @internal */
export type HomeAddress$Outbound = {
  street_1: string;
  street_2?: string | undefined;
  city: string;
  state: string;
  zip: string;
  country: string;
};

/** @internal */
export const HomeAddress$outboundSchema: z.ZodType<
  HomeAddress$Outbound,
  z.ZodTypeDef,
  HomeAddress
> = z.object({
  street1: z.string(),
  street2: z.string().optional(),
  city: z.string(),
  state: z.string(),
  zip: z.string(),
  country: z.string().default("USA"),
}).transform((v) => {
  return remap$(v, {
    street1: "street_1",
    street2: "street_2",
  });
});

export function homeAddressToJSON(homeAddress: HomeAddress): string {
  return JSON.stringify(HomeAddress$outboundSchema.parse(homeAddress));
}

/** @internal */
export type SignatoryCreateRequest$Outbound = {
  first_name: string;
  last_name: string;
  title: string;
  phone: string;
  birthday: string;
  email: string;
  ssn: string;
  home_address: HomeAddress$Outbound;
};

/** @internal */
export const SignatoryCreateRequest$outboundSchema: z.ZodType<
  SignatoryCreateRequest$Outbound,
  z.ZodTypeDef,
  SignatoryCreateRequest
> = z.object({
  firstName: z.string(),
  lastName: z.string(),
  title: z.string(),
  phone: z.string(),
  birthday: z.instanceof(RFCDate).transform(v => v.toString()),
  email: z.string(),
  ssn: z.string(),
  homeAddress: z.lazy(() => HomeAddress$outboundSchema),
}).transform((v) => {
  return remap$(v, {
    firstName: "first_name",
    lastName: "last_name",
    homeAddress: "home_address",
  });
});

export function signatoryCreateRequestToJSON(
  signatoryCreateRequest: SignatoryCreateRequest,
): string {
  return JSON.stringify(
    SignatoryCreateRequest$outboundSchema.parse(signatoryCreateRequest),
  );
}

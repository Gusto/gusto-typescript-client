# IntegrationType

The `integration_type` used for the report, if `aggregation` was 'integration.' Otherwise, this will be null.

## Example Usage

```typescript
import { IntegrationType } from "@gusto/embedded-api-v2025-06-15/models/components/generalledgerreport.js";

let value: IntegrationType = "qbo";
```

## Values

```typescript
"xero" | "qbo"
```
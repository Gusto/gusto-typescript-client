/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

/**
 * A single payroll tax liability rolled up into a tax payment
 */
export type TaxPaymentLineItem = {
  /**
   * Unique identifier of the payroll this liability came from
   */
  payrollUuid: string;
  /**
   * Unique identifier of the tax type this liability is for
   */
  uniqueTaxId: string;
  /**
   * The amount of this liability included in the tax payment
   */
  amount: string;
};

/** @internal */
export const TaxPaymentLineItem$inboundSchema: z.ZodType<
  TaxPaymentLineItem,
  z.ZodTypeDef,
  unknown
> = z.object({
  payroll_uuid: z.string(),
  unique_tax_id: z.string(),
  amount: z.string(),
}).transform((v) => {
  return remap$(v, {
    "payroll_uuid": "payrollUuid",
    "unique_tax_id": "uniqueTaxId",
  });
});

export function taxPaymentLineItemFromJSON(
  jsonString: string,
): SafeParseResult<TaxPaymentLineItem, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => TaxPaymentLineItem$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'TaxPaymentLineItem' from JSON`,
  );
}
